# A4 — The Obligation Engine Arc

**Version:** v21 · **Date:** 2026-08-14
**v21 :** ⚠️★★★ **L'ORDRE DU PLAN A ÉTÉ RENVERSÉ, ET LA MESURE QUI LE JUSTIFIAIT ÉTAIT FAUSSE — §9t CORRIGÉE, §9u AJOUTÉE.** Le *« zéro `d` négatif à aucune horloge »* de v20 était un **artefact du choix des horloges de sonde** : les cinq tombaient hors de la fenêtre négative (Wick, 2026-10-08 → 2026-12-31), trois au 1ᵉʳ janvier. **Wick atteint `d=−55` au 1ᵉʳ décembre.** L'axe de conséquence a donc été livré EN PREMIER — **C1 `5721871`, C2 `22b9fa9`, §9u** — et **(iii) n'est pas livré**.
**v20 :** ★★★ **LE PLAN D'ATTAQUE EST ARRÊTÉ — §9t.** Trois lots : **(iii) l'ajout fédéral · puis l'axe de conséquence · puis (ii) + l'alerte art. 52.** ★★ **Et (iii) est le PRÉALABLE des deux autres, mesuré :** `dueDate` recalcule l'anniversaire de l'année en cours, donc **la ligne fédérale se répare toute seule chaque 1ᵉʳ janvier** — la seule ÉLEVÉE calculable du produit n'est en retard que quelques semaines par an. *13 lignes fédérales dans le harnais, **zéro `d` négatif à aucune horloge**.* · ✅ **(ii) EST DÉBLOQUÉ** — Harvey a fermé l'art. 52 : les bornes se dérivent de la fin d'année d'imposition (P-44.1 r. 1, art. 3 par. 1°).
**v19 :** ✅ **`12ebd51` — (E)/(F1) EST LIVRÉ, §9s AJOUTÉ.** Le ranker lit enfin l'horloge qu'il recevait déjà : un champ portait DEUX responsabilités, et la nuller pour éteindre la première perdait la seconde. ★ **Et le seau ne bouge pas pour une raison qu'il faut énoncer précisément — la formulation évidente est FAUSSE** : le seau EST dérivé de l'horloge, en amont ; ce feeder la nullait en aval. **(F1) rétablit un accord qui existait déjà.** · ★★ **FACT 6 est le seul des sept dont on SACHE qu'il fonctionne — il a échoué deux fois avant de passer, dont une par sa propre faute de construction.** · ⚠️ **CE LOT NE REMONTE PAS LES MANQUEMENTS PROLONGÉS** — ils sont enterrés par l'ORDRE DES SEAUX. **Le lot suivant est la CONSÉQUENCE comme axe de classement** : la condition de la décision du 2026-07-05 n'a jamais été implémentée.
**v18 :** ✅ **`19d86b8` — (C2) EST LIVRÉ. Le geste (i) de (F) est fermé.** Le faux positif de l'année de constitution est corrigé, l'assertion cadence↔mode passe à SENS UNIQUE, et **FACT 5** — table exécutable de la frontière — est en place, **prouvé rejetant dans les deux sens**. ★ **Le crash latent que §9q décrivait est parti avec, dans le même commit : c'était la seule façon.** · ⚠️ **RESTENT (ii) LE RETRAIT QUÉBÉCOIS et (iii) L'AJOUT FÉDÉRAL** — voir §9p. FACT 5 est le **critère d'acceptation** de (iii) : il affirme un ENSEMBLE de lignes, donc il grandit avec la multiplicité.
**v17 :** ★★★ **LE CORRECTIF QUE L'ASSERTION NOMMAIT EST IMPOSSIBLE — §9q AJOUTÉ.** Le `DECLARED mode field` que §9p reprenait de l'assertion a été posé en worktree et mesuré : **il ne peut pas exister.** Un booléen doit répondre une fois là où la règle a deux comportements. ★ **Forme retenue : (C2) — l'assertion à SENS UNIQUE + un nouveau FACT 5, table exécutable de la frontière, prouvé rejetant dans les DEUX sens.** · ★★ **Et l'argument qui décide regarde vers 4b : FACT 5 affirme un ENSEMBLE de lignes (année de constitution → 0 · année+1 → 1 · horloge 2033 → 7) — c'est le critère d'acceptation dont la multiplicité fédérale a besoin.**
**v16 :** ★★★ **(F) A ÉTÉ INVESTIGUÉ ET CE N'EST PAS CE QUE CE PLAN DÉCRIVAIT — §9p AJOUTÉ, LIRE AVANT DE SCOPER 4b.** Trois faits mesurés renversent le cadrage : **le Québec n'a RIEN à itérer** (le feeder d'échéances émet déjà l'occurrence courante) · **le fédéral n'éclate NULLE PART** (0 ligne complétude, sa moitié est board-suppressed) · et **corriger le faux positif de l'année de constitution FAIT JETER l'assertion cadence↔mode**, donc le dashboard ne rend plus pour toute société sans exercice clos. ★ **Le pari de la phase 4 — « la cadence encode le mode » — vient de perdre, mesuré : une règle vivante sur deux.** · ⚠️ **Les cinq sites de §6/§8 ne forment PAS un bloc** : ils se répartissent sur deux gestes opposés qui ne partagent aucun site. Le site 4 est mal situé. · ⚠️ **L'art. 52 n'est pas calculable — notion absente du modèle.**
**v15 :** ✅ **`968a7ae` — LE CAP DE CURRENCY DU LIVRE. Le board conseille enfin dans le bon sens.** Rang 1 de Wick = Rapport annuel Corporations Canada, zéro fondationnelle dans le top-5, caméra Dom FR + EN · ⚠️⚠️ **LES TROIS PISTES ÉCARTÉES DE v13/v14 SONT PÉRIMÉES — §9n EST RÉÉCRIT.** La vraie cause n'était pas `VIRTUAL_URGENCY_FLOOR` : **le seau est ABSOLU** (`rank.ts:243-248`, retour immédiat), donc **aucune passe de réglage ne pouvait fonctionner** — et cette prescription était écrite dans ce plan, dans le Queue et dans Core · ★ **LA PHASE 4b DEVIENT PORTEUSE** : Harvey a établi que l'exposition fédérale à la dissolution **ne se prescrit pas**, donc chaque année non produite bloque la sortie de l'état de défaut · ★ **DEUX FIXTURES AJOUTÉES** (Café du Coin, Fixture Cap) pour couvrir l'axe de l'ÂGE · ⚠️ **LA PRÉDICTION DU 2026-10-08 EST CADUQUE** — elle portait sur un état que le cap a remplacé.
**v14 :** ✅ **`18558b6` — le rapport annuel fédéral peut enfin être en retard** (report retiré, ancrage à anniversaire + 60 j) · ⚠️ **HARVEY A CORRIGÉ NOTRE LECTURE : le fédéral se règle ANNÉE PAR ANNÉE** — l'art. 212 définit le seuil de sanction, pas le mode de régularisation · ★ **LE CLASSEMENT RESTE ENTIER** — deux ships ont corrigé des FAITS, aucun n'a touché l'ORDRE (§9o) · **PRIOR :** **Version:** v13 · **Date:** 2026-08-10
**v13 :** ✅ **`5adc6cd` — le doublon de la déclaration initiale est corrigé** (`OVERLAP_MERGE` élargi à `'once'`) · ★ **LA CADENCE DÉSIGNE LE PROPRIÉTAIRE DE LA LIGNE** — `per-fiscal-year`/`once` → fusion, `anniversary` → suppression ; les deux mécanismes **partitionnent** les clés, gardé par FACT 4 · ⚠️ **LE CLASSEMENT DU BOARD EST LE SUJET OUVERT, et la cause n'est PAS le bucket** — `VIRTUAL_URGENCY_FLOOR = 0.55`, dont la raison d'exister a disparu quand Harvey a jugé les documents constitutifs sans conséquence (§9n) · **PRIOR :** **Version:** v12 · **Date:** 2026-08-01
**v12 :** ⚠️ **LE MODÈLE DES DEUX FAMILLES A ÉTÉ TRANCHÉ (Core) ET IL PRÉCÈDE 4b.** Une obligation de DURÉE NEUTRE n'est jamais « en retard » — Harvey VERT — et relève d'un CAP par société. Une obligation DATÉE garde son échéance. **Rien de 4b ne se scope avant que l'état « pas à jour » existe.** · Le PLANCHER fondationnel n'a plus de justification et tenait Boundary B — fait à traiter, non objection. · **PRIOR :** **Version:** v11 · **Date:** 2026-07-31 · **Status:** **PHASES 1, 2, 3, R-1, R-2 ET 4a LIVRÉES**, plus `76a6eca` (liveness du jumeau) et `220fbdb` (outillage, **non déployé**). ⚠️⚠️ **LA PHASE 4b TELLE QU'ÉCRITE DANS CE PLAN EST MORTE — voir §9l AVANT DE LA SCOPER.**
**v11 :** ★ **4b disait « les lignes historiques gagnent des horloges » ; la règle de Dom dit qu'elles QUITTENT le board** — le travail est l'inverse de ce qui est écrit ici · ⚠️ **les chiffres 56 et ~+50 sont des BORNES SUPÉRIEURES**, dérivées avant la règle · ★ **l'itération par année n'est peut-être plus nécessaire du tout** · §9l ajouté.

**EN-TÊTE SUPPLANTÉ (v10), conservé pour que le cadrage ne soit pas réécrit en silence :** **Version:** v10 · **Status:** **PHASES 1, 2, 3, R-1, R-2 AND 4a SHIPPED** (`5e4ba52`, `fefb25a`, `74ae856`, `111c21c`, `41158de`, `c587441`). **NEXT: 4b — ITERATION, and the first phase since phase 1 where DOM'S CAMERA IS A REAL GATE.**
**v10 changes:** 4a shipped — §6's table updated, §9k added · **★ PHASE 4 IS SPLIT THREE WAYS, NOT TWO** (4a genericity / 4b iteration / 4c annual_meeting — the seam is WHICH BEHAVIOUR, not which rules; the row COUNT alone distinguishes them) · **★ DOM'S CLOSED-YEARS-ONLY RULING is locked into 4b's scope** · ⚠️ **THE ROW-COUNT PREDICTIONS IN §6/§8 ARE UPPER BOUNDS** — the 56-row and +50-row figures both predate that ruling and include the current OPEN fiscal year; **re-derive at 4b's investigation, do not inherit** · **★ THE PRE-INCORPORATION RISK IS SMALLER THAN THIS PLAN CLAIMED** — `0f7deee` cannot recur by construction (§9k).

**SUPERSEDED HEADER (v9), kept so the framing is not silently rewritten:** **Version:** v9 · **Status:** **PHASES 1, 2, 3, R-1 AND R-2 SHIPPED** (`5e4ba52`, `fefb25a`, `74ae856`, `111c21c`, `41158de`). **NEXT: PHASE 4 — the generic loop, and the first phase since phase 1 with a REAL CAMERA GATE.**
**v9 changes:** R-2 shipped — §6's table updated, §9j added · **★ THE MODULE IS NOW `lib/obligations/obligation-registry.ts` and the types are `Obligation*`; `filingFor*` is `ruleFor*`.** Passages below written before 2026-07-30 use the old names **deliberately, as history** — they were true when written (§9j). · **★ BOTH R PHASES HAVE LANDED, so the arc's one hard ordering constraint — "both before phase 4" — is SATISFIED.**

**SUPERSEDED HEADER (v8), kept so the framing is not silently rewritten:** **Version:** v8 · Next: **R-2**, then **phase 4**.
**v8 changes:** phase R **SPLIT into R-1 (shipped) and R-2 (pending)** on a measurement · §6's table gains both · §8 records that **R-1 resolved `annual_meeting`'s DISQUALIFYING deferral reason** and that **exposure is registry-driven at ONE of four assertion sites, not all four** · §3f's consumer list corrected (6 claimed, **9 exist**) · §9i added.

**SUPERSEDED HEADER (v7), kept so the framing is not silently rewritten:** **Version:** v7 · **Date:** 2026-07-30 · Next: **phase R**, then **phase 4**.
**v7 changes: ONE CORRECTION, NO NEW CONTENT.** §8's fixture-gap bullet is **STRUCK** — it asserted as an open risk the very thing §8's own v5 retirement note, ten lines above it, records as closed. Nothing else in this document is touched, deliberately: the phase-R framing question now in flight is a **hypothesis under measurement**, and this plan does not carry unmeasured claims (§9f — a figure written beside a *"Proven:"* claim is where a wrong number does most damage). It lands here when the investigation returns.

**SUPERSEDED HEADER (v6), kept so the framing is not silently rewritten:** **Version:** v6 · **Date:** 2026-07-29 · **Status:** **PHASES 1, 2 AND 3 SHIPPED**. Next: **phase R**, then **phase 4**.
**v6 changes:** phase 3 shipped and measured (§9h) · **§7 REWRITTEN — the stored-baseline gate is RETIRED in favour of a detached-worktree comparison, after a THIRD baseline death; the fresh capture is CANCELLED** · §8 gains the phase-4 camera warning and the `@deprecated` gate.

**SUPERSEDED HEADER (v5), kept so the framing is not silently rewritten:** **Status:** **PHASES 1 AND 2 SHIPPED** (`5e4ba52`, `fefb25a`). Both gating decisions taken and CONFIRMED. **A RENAME PHASE (R) NOW SITS BETWEEN 3 AND 4.** Phase 3 is next and needs a read-only investigation first.
**v5 changes:** Dom's recorded JUSTIFICATION for the arc (§1) · both §4 decisions marked taken · phase 2 shipped and measured (§9g) · **the phase table gains phase R** · §7 amended — **the reference capture is compromised as a whole-file baseline and a fresh one is a phase-4 PREREQUISITE** · §8 gains the four things phase 2's fields cannot express and retires three resolved items · §9f's open question CLOSED.
**Author:** Max, from CC's investigations 2026-07-28/29
**HEAD at drafting:** `e9bc5fb` (+ `8a0bf79`, docs-only, unpushed)
**Supersedes in scope:** the "feeder consolidation" framing in ZK_Queue. That entry sized the work from the merge; this one sizes it from the emission.

> **★ SYNC RULE (boot protocol):** this file lives in BOTH `docs/` and project knowledge. Amend one, amend the other, and bump the version above. Max reads project knowledge; CC reads the repo; neither can see the other's.

---

## 1. Why this exists

Four ranking patches were considered and rejected on 2026-07-29. Dom: **"there is no point of having a fix that is not a global solution."** He also asked, and it is the honest question: *"our entire system is so overwhelming and complicated — is it me?"*

It is not. Every defect found across two days traces to **one decision**, and this arc removes it. The plan exists so that a fresh Max does not re-derive three hours of measurement.

---

## 2. ★★ THE ROOT CAUSE — one decision, three symptoms

**`CompanyComplianceInput` is deliberately pure — "no I/O" — so `deadlines.ts` cannot see the checklist or the fiscal-year set.** The purity is real, documented in the file's own comments, and was defensible. Its running cost is roughly **one interface field per new obligation, forever.**

Three consequences, all previously chased as separate bugs:

1. **One boolean per obligation in the input.** `hasLaterAnnualFiling`, `currentFedReturnFiled`, `noPriorAnnualMeetingRecorded` — a fourth obligation means a fourth flag.
2. **One hand-written block per obligation in the body.** The feeder must be taught each rule's name.
3. **One clock for one year**, because only one anchor is passed in — which is why six of seven historical filing years are permanently dateless, and why the board cannot see them as overdue.

**★ The irony is recorded in the code:** `ANNUAL_MEETING_RECORD_KEYS` is *exported from* `deadlines.ts` so the caller can compute a fact and hand it back. **The feeder already owns the knowledge; it just isn't allowed to hold the data.**

---

## 3. MEASURED FACTS — do not re-derive these

All measured by CC 2026-07-28/29 at `e9bc5fb`. Line numbers are snapshots; re-anchor before use.

### 3a. `deadlines.ts` is a hand-written switch

- **It never iterates `FILING_REGISTRY`. Not once.** Three hardcoded string literals reach it: `filingForRuleKey('qc_initial_declaration')`, `('qc_req_annual_update')`, `('fed_annual_return')`. **The registry is a lookup table the switch reads, not a table the engine walks.**
- **Adding a registry entry today emits nothing.**
- **389 lines: ~61% per-obligation special-casing, ~36% shared machinery.** The four emission blocks are 199 lines (51%). Executable-only: ~84 bespoke vs ~73 shared. **Control flow is 100% bespoke.**
- **Four obligations, four hand-written `if` blocks:** RE-200, QC REQ annual update, federal annual return, annual meeting.
- **★ `annual_meeting` has NO registry entry at all** — `ruleKey: 'annual_meeting'` is a bare literal, its statutory basis an inline three-branch ternary. Deliberate: the registry is documented as *"the single source of truth for **government filing** obligations"* and the annual meeting is internal governance.
- All eight title strings are **inline literals** in the feeder, not i18n keys.

### 3b. The three flags are not structurally required

All computed in `app/[locale]/dashboard/page.tsx` from `completeness.checklist`. **All three are `checklist.some(...)` predicates the feeder could evaluate itself.** `currentFedReturnFiled` even hardcodes `'cbca_annual_return'`, which the registry already declares as `fed_annual_return.requirementKeys`. **The caller restates what the registry knows.**

### 3c. One-row-per-rule is INCIDENTAL, not essential

- The emission is a single `push` guarded by `if (fyEnd)` at the most recently completed FY. **The guard does double duty: "has any FY closed?" and "here is the only year I care about."**
- **`push` already takes `yearSeg` and `year` as parameters** and builds `id = deadline:{ruleKey}:{yearSeg}`.
- **★ `mergeObligations` already indexes twins by `` `${ruleKey}|${year}` `` — a map built to pair N deadline rows with N completeness rows. It has simply never received more than one.**
- **`fiscalYears: { year, endDate }[]` already exists** on `RequirementCompletenessResult`, built for each active year, and is **the same set the completeness checklist fans out over** — so a per-year loop guarantees a twin for every completeness row.
- ⚠️ **Caveat:** those `endDate`s are composed from the company's *current* year-end for every historical year. A year-end change retroactively rewrites them. `completedFiscalYearEnd` has the same flaw today, so this is not a regression — but scaling from one anchor to N **multiplies the blast radius**. See the deferred fiscal-year work in ZK_Core.

### 3d. What the registry already does right — the reason this is reachable

- **`dueDate(ctx)` is already a name-free pure function.** `qc_req_annual_update` is `(ctx) => ctx.fyEnd ? addMonthsClamped(ctx.fyEnd, 6) : null`. **Hand a generic loop an `fyEnd` and a rule and it computes the date with no name-awareness.**
- **`cadence` already DRIVES behaviour**, not merely describes it: `OVERLAP_MERGE` and `_boardSuppressedKeys` both derive from it. Working proof the pattern holds.
- Four consumers that used to drift (`OVERLAP_MERGE`, `isExternalRequirementKey`, `isBoardSuppressedRequirementKey`, rank's prerequisite resolver) are already views onto one table. **The registry fix worked. The emission never got the same treatment.**

### 3e. The registry's gaps — what a generic loop needs and cannot get

1. **Applicability** — no `frameworks` field; `framework === 'CBCA'` lives in `if` conditions
2. **Exposure / actionKind** — hardcoded per push
3. **Titles** — both locales inline in the feeder
4. **A suppression predicate** — no declarative home for the three booleans
5. **Upload-attach fields** — `requirementKey` + `canUpload` hand-set on the federal push only
6. **Multi-limb statutory basis** — `annual_meeting` needs three, chosen by framework and limb
7. **`FilingDueCtx` has no per-year `fyEnd`** — it carries the singular anchor

**★★ CORRECTED AT v8 — THIS DOCUMENT'S CONSUMER LIST WAS WRONG: 6 CLAIMED, 9 EXIST, 3 MISSED, 0 PHANTOM** (measured at R-1's investigation). The three missed are **`app/[locale]/dashboard/page.tsx`** (imports `filingFiscalYear`), **`components/ui/useObligationModalContent.ts`** (`import type CopyKey, ReasonKey`) and **`lib/obligations/obligation.ts`** (the reverse type edge). **The plan's six are all under `lib/`; the missed three are a route, a client component and the reverse edge — so the rename does NOT stay inside `lib/`, and the list concealed exactly that.** None could make R-1 non-inert (one date helper, two erased type imports). ⚠️ Also: *"five exported functions"* is defensible as **five to change**, not five that exist — **the file exports 9 functions and 17 symbols.**

### 3f. The board, measured

- **The board caps at 5** (`A3Board.tsx:34`). "Show more" is a `<Link>` to Complétude — **it never expands in place.** Both are per spec D4 and correct.
- **`LIVENESS_RANK` is an ABSOLUTE primary sort key above the score** (`rank.ts:104-115`), introduced by `c5c0637` (2026-07-06) — **three days after the ranking spec, which has no such concept.** It contradicts two locked decisions in their own words: D1 *"NOT a fixed category order"* and D3 *"a genuinely imminent government deadline can still pass above them."*
- **It fixed a real defect** — *"ancient-overdue items outranking current actions"*, verified as *"2026 resolutions above 2018 filings."* But **the root cause is `rank.ts:90`, a flat overdue ceiling** (`if (d < 0) return URGENCY_MAX`): one day late and eight years late score identically, then tie-break rung 4 *"oldest year first"* actively promotes the older. **The ancient item won on the tie-break, not the merits. Line 90 is untouched at HEAD.**
- **★ It was verified on Acme only** — the one fixture that structurally cannot show the side effect, since Acme has zero unsatisfied foundational rows on the board. **The inversion needs an empty book, and Acme is the opposite of one.**
- **The "Part-1 liveness multiplier" the comment says it replaced never existed** (`git show c5c0637^` — zero liveness references). **The weighted form was skipped entirely.**
- Scores confirmed: `STAKES_EXTERNAL=1.0`, `STAKES_FOUNDATIONAL=0.9`, `STAKES_INTERNAL_ANNUAL=0.6`, `STAKES_LOW=0.3`, `URGENCY_MAX=1.0`, `URGENCY_FLOOR_LOW=0.15`, `VIRTUAL_URGENCY_FLOOR=0.55`, `ESCALATION_WINDOW=45`. **`0.4950 = 0.9 × 0.55.`**
- **Acme's REQ update for 2025 — 27 days overdue, external — scores `1.0000`, the highest in either fixture, and ranks 2**, beneath a `0.0900` share-issuance draft that isn't due. **The scoring model worked and the bucket overrode it.**

### 3g. ★ The modelling that decides the sequence

A verified re-sort probe (self-checked three ways — identity, reversed, stride-7 — reproducing today's order exactly) modelled **`remediate` last; `live` and `regularize` compete on score**:

- **ACME: fixed.** The `1.0000` REQ update moves 2 → 1. Top 5 becomes REQ update, annual meeting, auditor waiver, director departure, officer departure.
- **★★ WICK: NOT ONE POSITION MOVES.** All eight founding documents stay at ranks 1-8. **A new company's board is identical before and after.**
- The ancient-overdue defect does **not** return: all 38 pre-2024 rows across both fixtures are `remediate` and stay demoted.

**Why Wick doesn't move:** its 2025 REQ update is `regularize` and **clockless at `0.1500`**. Give it a clock → `1.0000`, but today's bucket keeps it at rank 19. Remove the bucket → still `0.1500`, below `0.4950`. **Neither fix alone moves a new company's board. Both together put the overdue filing at rank 1.**

### 3h. The seam is in the wrong place

**Nothing is epistemically irreducible between the two engines.** Given the same inputs — checklist, fiscal-year set, company scalars, registry — one function could compute every field both produce. **`OVERLAP_MERGE` is the receipt for the split.**

**Two feeders remains the floor** — but on **cadence**, not on completeness-vs-deadline: `per-fiscal-year | anniversary | once` are all **calendar-instantiated**; `event` is **act-instantiated**. Different instantiation sources, genuinely two shapes. **`cadence` already names the correct axis; it just doesn't drive emission yet.**

---

## 4. ★★ TWO DECISIONS GATE THE ARC — Dom's, pending

### D-A · Multiplicity: cadence wins — and it must land FIRST, not last

Dom ruled months ago that **registry `cadence` beats catalog `category`**, but sequenced it *last* because it moves a visible number. **CC's finding reverses the sequencing:** a generic engine driven by `cadence` hits the disagreement on day one (`fed_annual_return` is `cadence: 'anniversary'` but `category: 'annual'`, so completeness fans it out and a derivation exists purely to discard the fan-out). **You cannot design around an unresolved data disagreement.**

**Cost, unchanged and already accepted in principle: Wick's `requirementsTotal` drops by seven and the completeness percentage moves.** Dom sees the number change deliberately.

**★ STATUS: CONFIRMED AND SHIPPED (`5e4ba52`, phase 1).** It landed first, exactly as CC's finding required, and the measured cost came in far below the estimate: **ONE catalog row, ONE framework.** 20 of 25 catalog rows have no `FilingRule` at all and 4 of the 5 that do already agreed with their cadence — only `cbca_annual_return` disagreed. **Wick `requirementsTotal` 49 → 42; ACME untouched end to end** (LSA never selects the key). **Do not re-open.**

### D-B · Does the registry hold INTERNAL obligations?

`annual_meeting` has no registry entry because the registry is scoped to *government filings*. **If it stays out, the switch survives** — one hand-written block instead of four, and every future *internal* obligation stays code.

**Max's lean: expand it.** The concept the product needs is **obligations**, not filings. Otherwise half of future work is still code changes, which is what Dom said he cannot afford.

**★ STATUS: RULED BY DOM — THE REGISTRY HOLDS INTERNAL OBLIGATIONS TOO, and the membership test is "DOES IT HAVE A CADENCE", not "is it filed with a government".** Max's lean was taken.
**★★ AND THE RULING HAS A COST NOBODY HAD PRICED, FOUND AT PHASE 2 — IT IS WHY PHASE R EXISTS.** `isExternalRequirementKey` means *"has a registry entry"* and is USED as a proxy for *"is external"*; `feeders/completeness.ts` reads it for exposure assignment. **So the first internal rule to join the table makes that function silently WRONG** — it would report the annual meeting as external. The correction is `exposure === 'external'`, **a behaviour change rather than a rename**, and it must not ride an additive phase. **The membership question was the easy half; the table still asserts in its own name, type names and header that everything in it is a government filing.**

---

## 5. THE TARGET

**Adding a new obligation = a catalog row + a registry entry. No code change.**

- The registry declares *what* an obligation is, *when* it is due, *how many* of it there are, and *when it does not apply*.
- **Two feeders, split on `cadence`:** calendar-instantiated and act-instantiated.
- One feeder produces the row **whole** — clock, document state, metadata — so `OVERLAP_MERGE` and the field union die by construction, taking the `helpKey`/`copyKey` drop with them.
- One liveness per row. One year unit.

---

## 6. THE PHASES

Each is separately shippable and separately gated.

| # | Phase | Ships | Gate |
|---|---|---|---|
| **0** | The two decisions (§4) | nothing | Dom |
| **1** | **Multiplicity: cadence drives the fan-out** | visible % change | ⚠️ Section D and the denominator BOTH move. Dom sees the number. |
| **2** | ✅ **SHIPPED `fefb25a`** — the registry gains its generic-emission fields (§3e, §9g) | inert | ★ `tsc` **proven to reject**; Section D byte-identical (WEAK — see §9g) |
| **3** | ✅ **SHIPPED `74ae856`** — the engine receives the checklist and the fiscal-year set (§9h) | inert | ★ `tsc` failed NATURALLY between the two edits; worktree comparison byte-identical |
| **R-1** | ✅ **SHIPPED `111c21c`** — the SEMANTIC SWAP: `exposure` ← the rule's `exposure`, `hasFiling` ← `actionKind === 'file_externally'`; `isExternalRequirementKey` DELETED | inert on today's data | ★ a **REJECTION PROOF**, not a green artifact — old code and new measured against one synthetic `exposure:'internal'` entry (§9i) |
| **4a** | ✅ **SHIPPED `c587441`** — GENERICITY: three blocks -> one loop, same rows out | inert | byte-identical WHOLE-FILE **plus** a rejection proof (§9k) — neither sufficient alone |
| **4b** | ⚠️ **MORTE TELLE QU'ÉCRITE — §9l.** Les lignes historiques QUITTENT le board au lieu de gagner des horloges. NON CONSTRUITE | **moves** | row count unchanged; N rows gain clocks — ⚠️ **re-derive N; the 56 figure is an UPPER BOUND** |
| **4c** | **`annual_meeting` joins the registry.** NOT BUILT — Harvey-blocked | **moves** | new rows; count changes; the three `@deprecated` fields go **3 -> 0** |
| **R-2** | ✅ **SHIPPED `41158de`** — THE RENAME: filing → obligation — 3 type names, `FILING_REGISTRY`, 4 `filing*` functions, the file path, and the table's identity claim | mechanical | Section D byte-identical — a **real** gate here, because a rename must not move output |
| **4** | **The generic loop** replaces the four blocks; **per-year clocks fall out of it** | the big one | ⚠️ Section D **moves on purpose** — see §7 |
| **5** | **The bucket:** `remediate` last; `live` and `regularize` compete on score | board reorders | Modelled result in §3g is the prediction to check against |
| **6** | **Foundational stakes gradation** (Harvey's FORT/MODÉRÉ/FAIBLE) | board reorders | ⚠️ needs more than the boolean — see §8 |

**Phases 2 and 3 are the inert-first discipline** (Lessons §41) — additive, provable, and they make phase 4 a behaviour change with everything already in place.

**★ PHASE R IS DELIBERATELY LETTERED, NOT NUMBERED.** Renumbering 4-6 would falsify every existing reference to "phase 4" — in ZK_Core, ZK_Queue, ZK_ShipLog **and in a code comment at `annual_meeting`'s bare `ruleKey` literal in `deadlines.ts`**, which names phase 4 as where that rule lands. A letter inserts the work without invalidating the references (Lessons §66's principle applied to a plan: anchor to something that survives editing).

**★ AND PHASE 3 NEEDS A READ-ONLY INVESTIGATION BEFORE IT IS SCOPED**, because one attribution is unsettled: CC's phase-2 verdict called the per-year loop *"code phase 3 still has to write"*, while the table above assigns the loop to **phase 4** and gives phase 3 the widening only. **Max propagated CC's framing before checking it against §6** — settle it from the code, not from either document (Lessons §64). The investigation must also decide whether phase 3 populates only `CompanyComplianceInput` or threads the set through to `ctx.fiscalYears` (inert either way), and how the **transient double source** is marked: after widening, the input carries the checklist AND three booleans derived from that same checklist until phase 4 deletes them — a flag shadowing a fact (§56) unless it is unmistakably marked as on its way out.

---

## 7. ★ WHAT "CORRECT" LOOKS LIKE — the acceptance criteria invert at phase 4

**★★★ AMENDED A THIRD TIME, 2026-07-29 AFTER PHASE 3 — THE STORED-BASELINE GATE IS RETIRED ENTIRELY AND THE FRESH CAPTURE IS CANCELLED.** `item4-before2/` became unreproducible **one day old** when two ACME director documents left the book mid-session. That is the THIRD death in three days, and the diagnosis is that the method was never sound: **a stored baseline compares `(code T1, data T1)` against `(code T2, data T2)` — two variables move at once.** It was a controlled experiment only for as long as the data happened to hold still.
- **THE REPLACEMENT: a detached `git worktree` at HEAD.** `(code HEAD, data NOW)` vs `(code HEAD+change, data NOW)` — one variable, minutes apart, data constant BY CONSTRUCTION. Non-destructive, `git stash`-free, no maintenance, cannot go stale.
- **★ FOR PHASE 4, WHOSE GATE IS SECTION D MOVING ON PURPOSE: run the harness THREE times — HEAD, tree, HEAD AGAIN.** Two matching HEAD runs prove the data held still for the duration, so any remaining diff is the code. **One extra run answers in thirty seconds what phase 3 spent an investigation on.**
- **Stored captures keep exactly ONE job: a historical record of a date.** Real — it is how phase 1's delta was attributed — but archaeology, not acceptance.
- ⚠️ **The `null(certified)` label fix SURVIVES and DECOUPLES from the capture** — it currently reports five MISSING documents as FILED to any future reader of an artifact.

**SUPERSEDED (v5), kept so the framing is not silently rewritten — AMENDED AFTER PHASE 2: THE REFERENCE CAPTURE IS COMPROMISED AS A WHOLE-FILE BASELINE, AND A FRESH ONE IS A PHASE-4 PREREQUISITE.** `item4-before2/` was captured at **13:45:33**; `5e4ba52` is dated **15:06:48** — **the reference predates phase 1 by 81 minutes and therefore CONTAINS phase 1's effect.** Phase 2's whole-file diff came back 139 lines apart in Wick's Sections C/E1/E2, and CC verified the attribution rather than assuming: it matches phase 1's own recorded numbers line for line (`requirementsTotal` 49 → 42, Section C 8 → 1, Section E2 8 → 1, `SUPPRESSED_BOARD_ANNIVERSARY` 32 → 4 across four clock blocks), ACME byte-identical.
- **It remains valid for the SECTION D BLOCK ONLY** — and only because phase 1 deliberately did not move Section D. **The gate was sound by luck of the previous ship's shape, not by construction.**
- **★ IT IS FATAL FOR PHASE 4, whose criterion is Section D moving ON PURPOSE:** the intended delta and the inherited delta would arrive in one diff with nothing to separate them. **This is the second baseline to die this way** (`item4-before/` was the first, and its hash "held across five ships" right up to becoming unreproducible).
- **THE SEQUENCE, IN THIS ORDER:** (1) **fix the harness's `null(certified)` label FIRST** — since `9a5c755` null also means *exempt*, so capturing first would bake *"five MISSING documents are FILED"* into the artifact every future comparison is measured against (§67); (2) capture at `fefb25a`; (3) **record the capture timestamp AND the HEAD inside the artifact**, not in a filename. Lessons §68.

**SUPERSEDED, kept so the earlier framing is not silently rewritten — Phases 2-3: Section D byte-identical, reference `327 rows, md5 `e35801c6…`, at `item4-before2/BEFORE-2026-07-27.txt`.** The earlier figure (`4438011d618b104b6ae1a4b056936ce1`, 317 rows, `item4-before/`) held unchanged across five ships and **can no longer be produced** — Wick gained three ranked rows when its federal-return receipt was deleted (§9e). **Gate against `item4-before2/`; the old hash would fail spuriously.** Any movement beyond that still means the change leaked.

**★ THE ROUTE GATE, restated to remove an ambiguity that has been quoted wrongly for five ships:** the correct figure is **19 pages + 21 API = 40 EXECUTABLE PATHS**, which renders as **41 TABLE ROWS** in the build output because `/_not-found` occupies a row. *"40 routes"* meant executable paths; a build reporting 41 rows is correct and unchanged. **State which you are counting.**

**★ Phases 4-6: Section D SHOULD MOVE, and that is the point.** The old brief's byte-identity target was wrong. Dom's framing decides it: **"ZapOkay should somewhat feel like a calendar."** In a calendar a row either **has a date** or is **honestly dateless**. Acme's FY2019 REQ update is neither — no date, status `open`, six years past due. **The six unclocked rows are a defect, not a design.**

So the phase-4 gate is a **deliberate before/after comparison**, not a hash:

- every fiscal year of a per-fiscal-year obligation carries a due date
- Acme's `1.0000` REQ update reaches rank 1 (phase 5)
- **★ WICK's overdue filings rise above the founding documents** — the criterion that matters, because Wick is the shape of every new customer
- ancient-overdue does **not** return: pre-2024 rows stay `remediate` and demoted
- the harness clock-invariance test still passes for foundational rows
- **Dom's camera is a real gate from phase 4 on** — unlike the four ships of 2026-07-28, this changes the main surface

---

## 8. OPEN QUESTIONS AND RISKS

**★★ TWO THINGS PHASE 4 MUST BE TOLD BEFORE IT STARTS.**

- **★ THE PRE-INCORPORATION RISK IS INVISIBLE TO THE CAMERA — say so to Dom explicitly, in advance.** `completedFiscalYearEnd` has THREE deliberately-different null-FY answers, now written down (§9h): `qc_req_annual_update` → NO ROW (art. 45 LPLE ties it to a COMPLETED fiscal year); `annual_meeting` → NO ROW (nothing to present before a year closes, and a not-yet-due obligation must never read as lateness); `fed_annual_return` → **STILL FIRES**, because its clock is anniversary-anchored and not fiscal at all. **Two suppress, one fires, and the one that fires does so for a reason unrelated to the fiscal year.** Flattening them into one generic loop resurrects `0f7deee`. **No fixture is a first-year company; the feeders are pure; a SYNTHETIC TABLE-TEST is the camera here** (Lessons §55).
- **★ `grep @deprecated deadlines.ts` → ZERO is part of phase 4's gate.** The three booleans are marked with `@deprecated` naming phase 4 as their remover, but **nothing enforces it**: no eslint deprecation rule is configured, and the codebase's own `@deprecated` precedent has sat marked *"vestigial, zero consumers"* since 2026-05-15 and is still present. A marking that depends on memory rots; a gate does not.


**★★ FOUR THINGS THE PHASE-2 FIELDS CANNOT EXPRESS — CC's closing verdict, ordered by when they bite. None is a defect today; all four are design inputs for phase 3/4.**

1. **`suppressWhenSatisfied` cannot express a CONJUNCTION, and `annual_meeting` needs one.** Its first-meeting predicate is `noPriorAnnualMeetingRecorded` **AND** `inc + 18mo` still in the future — a satisfaction test *and* a clock test. **Phase 4 needs a field that can HOLD one**, not merely a verification that the two halves are one predicate. ⚠️ If the first half alone were mapped onto `suppressWhenSatisfied`, the annual meeting would be suppressed **forever** once any annual shareholder resolution is satisfied — and on a per-fiscal-year cadence, for every later year too. **Design it; do not discover it.**
2. **`titleKey` alone cannot produce a rendered title.** The year segment comes from the `yearSeg`/`o.year` logic in the feeder's `push`, including the deliberate `yearSeg === 'anniversary' → null` rule. **So the yearSeg/display-year derivation is a HARD PREREQUISITE of using `titleKey` at all**, not the independent nicety it was previously banked as.
3. **`frameworks` is capped by what reaches it.** `CompanyComplianceInput.framework` is the two-member derived view — LSAQ has already collapsed into LSA upstream — so an entry needing to distinguish them cannot, whatever it declares. *Qualification: if LSAQ and LSA are one regime under two spellings the collapse is CORRECT and the cap is harmless; the risk is a genuine third framework.* **Banked separately: are they one regime? Possibly Harvey's.**
4. **★★ NOTHING EXPRESSES "iterate per year, BUT SKIP PRE-INCORPORATION YEARS" — the largest correctness risk in the arc's remainder.** `cadence` gives the intent and `fiscalYears` the set, but the guard `completedFiscalYearEnd` performs today is **per rule, with three different declared answers**, and ZK_Core records its `null` return as load-bearing *precisely because it forces every consumer to declare its own answer rather than inherit a silent wrong default.* **Flattening three deliberate answers into one generic loop resurrects `0f7deee` — phantom overdue rows for first-year companies.** ⚠️ **NO FIXTURE IS A FIRST-YEAR COMPANY**, so this is invisible to both fixtures AND to Dom's camera. The feeders are pure → **a synthetic table-test IS the camera, and Dom must be told so explicitly before the phase rather than after** (Lessons §55).

**RETIRED FROM THIS SECTION AT v5, resolved rather than deleted:** the `exempt_from_lateness` boolean-vs-three-tiers gap and the phase-6 product question both stand (below). **What is newly answered:** the registry CAN express applicability, exposure, verb, title and presumed-discharge (phase 2), and the fixture gap on *"a CBCA company without `hasLaterAnnualFiling`"* **closed by accident** when Wick was emptied — see §9e.


- **★ `exempt_from_lateness` is a BOOLEAN and Harvey's gradation has THREE tiers.** Phase 6 needs either a second column or a graded one. **The plumbing people assume is there is not.**
- **★ A product question inside phase 6:** the spec locks `EXTERNAL > FOUNDATIONAL`. Harvey called the share subscription *exposition réelle* — proof of who owns the company. **Does that outrank an overdue government filing?** Arguably yes before a sale, no against a radiation risk. **Not a tuning constant — it amends the spec.**
- **`rank.ts:90`, the flat overdue ceiling, is untouched.** Both `c5c0637` and phase 5 route around it. Decaying ancient urgency would let the score express what the bucket currently partitions. **Priced but not scoped.**
- **★ The ranking spec needs a v2.** `a3-ranking-brain-spec-2026-07-03.md` **predates the liveness axis entirely** — no vocabulary for `regularize`/`remediate`, and it never says how the axis feeds the score. **That gap is what let `c5c0637` add an absolute primary sort without anyone noticing it contradicted D1 and D3.**
- **The year-end-change caveat** (§3c) — per-year anchors multiply an existing known flaw. Say it out loud rather than widening it silently.
- **Harvey, still open:** is an unfiled REQ annual update from 2019 still legally owed, or extinguished by later ones? **Decides whether the historical rows become `overdue` or should stop existing.**
- **~~A fixture gap: neither Acme nor Wick is a CBCA company without `hasLaterAnnualFiling`~~ — STRUCK AT v7. THE CLAIM IS FALSE AND WAS ALREADY FALSE WHEN IT WAS LEFT STANDING.** Wick **is** that company and lacks `currentFedReturnFiled` too, so the RE-200 deadline-twin path `ddf061d` guards **and** the federal clear-gate's reopen behaviour are live and measurable — §9e, and the retirement note ten lines above this bullet. **Both halves shipped in the same v5 amendment: the correction was added and the original was left in place**, which is Lessons §62 — two versions of one fact in one document are worse than the original over-claim, because a reader resolves the contradiction by trusting whichever half suits them. **Do not re-add this risk, and do not restore Wick's document to close a gap that is already closed.**

---

## 9a. ★★ FINDINGS FROM THE PHASE 1 MEASUREMENT (2026-07-29) — all independent of the arc

**Phase 1 measured smaller than the plan assumed: ONE catalog row, ONE framework.** 20 of 25 catalog rows have no `FilingRule` at all; 4 of the 5 that do already agree with their cadence. **Only `cbca_annual_return` disagrees** (category `annual`, cadence `anniversary`). Acme does not move — it is LSA and never selects that key. **Wick `requirementsTotal` 49 → 42.** **★ THE PERCENTAGE FIGURES BELONG TO A FIXTURE THAT NO LONGER EXISTS — state which fixture, never a bare number.** `2.041% → 2.381%` is `1.0/49 → 1.0/42` and is **arithmetically correct for the `item4-before/` state**, when Wick held one uploaded, certified FY2026 federal return. **Against the CURRENT fixture the percentage does not move: `0.000% → 0.000%`, because Wick's numerator is 0.** Both are true of different fixtures. ⚠️ **Max got this wrong three turns running** — asserted the old figures, over-corrected to "numerator is 0", then reverted to "1.0 from a generated document", which `STATE_WEIGHT` makes **arithmetically impossible** (`généré = 0.5`, so a numerator of exactly 1.0 can only be one UPLOADED document). CC refused to write the unverified figure into a commit beside a *"Proven:"* claim. **The −7 denominator move is the real, reproducible finding and is unaffected.**

**★ A DESIGN DECISION LIVES INSIDE PHASE 1, and it reads in §6 as a pure data change.** The engine has TWO loops — foundational (`year: null` + the lateness floor) and annual (per-FY). **`anniversary` fits neither.** Dropping the survivor into `foundationalReqs` would inherit the floor and make a *correctly filed, current* federal return read `regularize` — a new falsehood traded for an old one.
**MAX'S RULING (CTO): a THIRD bucket.** `cadence: 'anniversary'` emits **one row**, at the year the deadline feeder already uses for its attach key (`filingFiscalYear`), with **NO foundational floor**. It must stay counted (*the book is the product*), it is not foundational (it has a clock), and it is not per-fiscal-year (one live instance). ⚠️ **UNVERIFIED: confirm `filingFiscalYear` yields the same year the completeness row carries, or the two will not merge and we get TWO rows where there is one today.**
- `_boardSuppressedKeys` (derived from `cadence === 'anniversary'`, existing purely to discard the wrong fan-out) **dies — but only if the surviving row is the deadline row or does not reach the board.** Depends on the ruling above.
- `OVERLAP_MERGE` is untouched by Phase 1; it serves the two REQ keys where category and cadence agree. It dies at Phase 4.

## 9b. ★★ THE ORPHANED-DOCUMENT CLASS — two producers, zero detectors

**A document whose `(requirement_key, requirement_year)` matches no checklist row becomes INVISIBLE IN COMPLÉTUDE, FULLY VISIBLE IN THE VAULT AND THE BINDER, and is silently subtracted from the completeness numerator.** Not data loss, not a broken link — **a document the user can see and download in two places and can never certify from the surface where documents are managed.** Nothing in the codebase detects or reports it.

**★ PRODUCER 1 — CHANGING `incorporation_type` IN SETTINGS. Real, measured, live in our own fixture.** Acme carries two orphans right now: `cbca_auditor_waiver_2024` and `cbca_first_board_resolution`. **Both were GENERATED IN-APP** (`source: 'generated'`, `uploaded_at: NULL`), through the normal UI flow, with proper `document_generated` log entries. The `activity_log` shows `incorporation_type` changed at 19:29:29 on 2026-06-17; the three documents generated after it were produced while Acme genuinely **was** CBCA — so the CBCA key was correct and *"art. 163 LCSA"* was the correct citation. **They became wrong RETROACTIVELY when the framework was set back.**
- **The generator does NOT filter by framework** (`generatePdfDocument.ts` selects the requirement by key alone, where the completeness engine filters `framework.eq.LSA,framework.eq.ALL`). **But adding that check would not have prevented this** — at generation time the key was valid. **The gap is that `incorporation_type` is editable with no handling for documents already keyed to the old framework.**
- **★★ THIS IS THE THIRD INSTANCE OF ONE ARCHITECTURAL DEFECT.** Core: *"a fiscal year's boundaries are a property of THE YEAR, not of THE COMPANY."* **A document's framework is a property of THE DOCUMENT — the regime it was created under — not of the company as it stands today.** Same shape as `fiscal_year_end_month/_day`, and the same family as Lessons §60. **There is a real corporate act behind it: CONTINUANCE** (a CBCA company continued under LSAQ and vice versa). Harvey has not been asked.

**PRODUCER 2 — PHASE 1 ITSELF**, deliberately: six per-year `cbca_annual_return` rows cease to exist. **On THIS database the risk is nil — zero `cbca_annual_return` documents exist for any company.** On a shipped product it would not be: that key has `can_upload = true` and those rows render with an upload affordance today.

**★ THE DETECTOR — priced, not built.** Two limbs: **FRAMEWORK ORPHAN** (`requirement_key` not in the company's framework ∪ `ALL`) and **YEAR ORPHAN** (`requirement_year` outside the active fiscal-year set, or a null/non-null mismatch against `category`). Joins over three tables on indexed columns, hundreds of rows per company — **milliseconds. The query is not the cost; deciding what the USER should do about them is.**
- **A SCRIPT beside `probe-consolidation.ts` — cheapest, hours, and the right FIRST move.** It answers the question Phase 1 actually needs before shipping to anyone with data: *how many real users would be affected.* It cannot fix anything and nobody runs it twice.
- **A COMPLÉTUDE SECTION** ("Autres documents / documents non rattachés") is the only option that closes the loop for the user, since the failure mode is precisely *visible in the vault, invisible where documents are managed*. Prior art: `f6534e9`'s binder "Autres documents" grouping. **Real UI work — bilingual strings, Aria, Dom's camera, and a product decision about what actions the row offers (re-key? delete? leave?).**
- An API route on its own: no. It earns its place only as the Complétude section.
- ⚠️ **A detector finds orphans; it does not tell the user what to do about them and it does not stop either producer.**

## 9c. ★★ THE SETTINGS PADLOCK IS ON THE WRONG FIELDS

`incorporation_type`, `neq`, `province` and `incorporationDate` are behind `unlockedFields` — a padlock that must be clicked, a confirmation to pass, and it **re-arms on every page load** (`useState<Set<string>>(new Set())`, per-render state). Changing the framework is therefore a **deliberate, confirmed act**, which downgrades Producer 1's likelihood without changing its nature.

**★ `fiscal_year_end_month` and `fiscal_year_end_day` ARE NOT GATED. They are as freely editable as the legal name.**

**That is backwards.** Per §3c and Core's deferred fiscal-year section, **historical `endDate`s are composed from the company's CURRENT rule, so changing the FY-end retroactively rewrites every historical due date.** It is **easier to trigger AND broader in blast radius** than the framework change, and it is the only one of the five with no friction at all.
- **The machinery already exists** — adding two fields to a gate that is already built, already confirming, already re-arming.
- **Harvey's ruling is NOT violated:** he ruled ZapOkay must not *validate* an FY-end change (the user or their accountant enters it; ZapOkay propagates). **A padlock is not validation — it makes a rare act deliberate.**
- ⚠️ **CORRECTED 2026-07-29 — an earlier draft of this section claimed "Acme's fiscal year-end was changed that day" from the 2026-06-17 19:29:29 log entry. THE DATA DOES NOT SUPPORT THAT.** Those fields appeared in `changed_fields` because they were written on **every** save of that form, touched or not — their presence is guaranteed, not evidential. Only `incorporation_type` is real evidence of intent, because it was gated. **★ AND THAT IS THE BETTER ARGUMENT FOR THE GATE: an ungated field that writes unconditionally leaves an audit trail that cannot distinguish "changed" from "saved". Gating it fixes the log as a side effect.** (CC's correction, taken.)

## 9d. ★ AN UNLOGGED HARD-DELETE PATH EXISTS IN PRODUCTION

**`app/api/cron/purge-superseded/route.ts` hard-deletes documents and Storage objects and writes NO `activity_log` entry** — its only record is a `console.log` that ages out of Vercel's function logs. **After it runs, there is no durable evidence the document existed.** On a product whose premise is *the book is the product*, that is a gap in its own right.

**It is NOT what removed Wick's document**, and the elimination is clean: the selection requires `status = 'superseded'` AND `superseded_at` non-null AND older than a 10-day buffer. Wick's federal return receipt (`ba08b816…`, uploaded 2026-07-25 01:47, logged) was `status = 'active'` and four days old. **So the deletion was a direct DB operation or another unlogged path.** A "test-data purge" named in that route's header comment has not been read — the obvious next place to look.

## 9e. ★ THE FIXTURE CHANGED MID-ARC — and it closed a gap by accident

**★ CORRECTED 2026-07-29 — WICK'S EMPTY STATE IS DELIBERATE. Dom emptied it to make it a genuinely fresh company: *"that was the goal".*** An earlier draft of this section framed it as an unexplained loss; it was intentional and the alarming framing is retired. **★ AND IT MAKES WICK A BETTER FIXTURE, NOT A DEGRADED ONE: zero documents is the shape of EVERY CUSTOMER ON DAY ONE** — precisely the case §3g proved the board fails, and the case phases 4-6 exist to fix. Read `0.000%` as the correct reading for a fresh company rather than as drift. ⚠️ **What does NOT retire: the unlogged hard-delete path (§9d) stands independently** — it was eliminated as the CAUSE on eligibility grounds *before* anyone knew the deletion was deliberate, and the gap never depended on Wick. **The facts of the change stand:** Wick lost one document between the `item4-before/` capture (2026-07-28 17:12) and 2026-07-29 — the certified `cbca_annual_return` for FY2026. **One deleted row moved 458 lines** across Sections A, C, D and E2, because it was the only row satisfying **two** caller-computed booleans at once — `hasLaterAnnualFiling` (un-suppressing the RE-200 on BOTH feeders) and `currentFedReturnFiled` (reopening the federal return's clear-gate). **An unplanned demonstration that the three flags §2 identifies as the root cause work exactly as designed.**

**Acme was byte-identical across the same window — the instrument is sound; this is data, not drift.**

**★ USE `item4-before2/` AS THE REFERENCE. `item4-before/` records a state that no longer exists.**

**★ DO NOT RESTORE WICK'S DOCUMENT.** §8 **formerly** recorded a fixture gap — *"neither Acme nor Wick is a CBCA company without `hasLaterAnnualFiling`"* — **struck at v7, because of this paragraph.** **Wick is now exactly that company, and lacks `currentFedReturnFiled` too.** Both previously unverifiable paths — the RE-200 deadline twin that `ddf061d` guards, and the federal clear-gate's reopen behaviour — are live and measurable for the first time. **The gap closed by accident; restoring the document would reopen it.**

## 9f. ★ PHASE 1 — SHIPPED `5e4ba52` (2026-07-29). What it proved, and what it taught.

**One row, one framework, five gates.** `cadence: 'anniversary'` now takes a **third bucket** in `requirement-completeness.ts` — one instance at `filingFiscalYear(...)`, no foundational floor, still counted, still uploadable, still binder-eligible. **Cadence wins where a rule exists; category continues where it does not** — 20 of 25 catalog rows have no `FilingRule` and were untouched.

**★ MAX'S RULING WAS RIGHT IN SUBSTANCE AND WRONG IN ONE QUARTER OF ITS RATIONALE, and CC stopped on it at Step 1.** The ruling said the surviving completeness row and the deadline row "still merge into one." **They cannot.** `OVERLAP_MERGE` derives from `cadence === 'per-fiscal-year'`, so an `anniversary` row is **not a key in it**, and `mergeObligations` returns at `if (!rk) return o` **before the year is ever consulted. Perfect year alignment cannot help a merge that never runs.**

**★★ WHICH INVERTED THE ACCEPTANCE GATE — Max had briefed it backwards.** The brief said *"Section D moves on purpose; confirm seven rows disappear."* **Wrong: all eight rows were ALREADY suppressed before emission** (Section A: zero obligations; Section C: eight `SUPPRESSED_BOARD_ANNIVERSARY` drops). **The board contained none of them and contains none after. SECTION D MUST BE BYTE-IDENTICAL** — and *that is a sharper claim than the one it replaced.*

**★ `_boardSuppressedKeys` SURVIVES, and Step 3's premise was inverted.** It looked like pure reconciliation for a fan-out that should never have happened. **It is now the ONLY thing keeping the surviving row off the board** — without it, the board gets TWO rows for one obligation, worse than the disagreement the phase fixes. **Recorded in the code so a future reader does not remove it as dead reconciliation.**

**THE MEASURED RESULT.** Section D byte-identical, 327 rows, md5 `e35801c6…`, both fixtures. **Acme byte-identical across its entire 213-line block** (LSA never selects the key). Wick: Section C drops **8 → 1**, Section E2 **8 → 1**, `requirementsTotal` **49 → 42**, verdict held. `tsc` 0, build clean, 40 executable paths.

**★ WHAT PHASE 1 PROVES FOR THE REST OF THE ARC — this is why it went first:** the registry **can** drive engine behaviour rather than merely describe it; **`cadence` is the correct axis**; and the change was **provable rather than plausible.** Phases 2-6 stand on that.

**★★ BANKED, AND IT HAS A TRIGGER DATE: the anniversary row's liveness is computed by a mechanism that is structurally meaningless for it.** Branch B is a *year-based proxy for lateness*, and an anniversary row's year is **current by construction** (`yearsBehind = 0` always), so **it can never escalate.** The right input is the **clock**, which the completeness feeder does not have — **the A4 root cause exactly.**
- **NOT a regression:** the surviving 2026 row already read `live` before this phase for the same reason. The escalation was carried entirely by the seven older rows — obligations Harvey ruled the company **does not owe** (which is why the cadence is `anniversary`). **A false signal was removed, not a true one.**
- ⚠️ **BUT THE TWO SURFACES AGREE TODAY ONLY BY COINCIDENCE OF DATE.** Wick's deadline row is **due 2026-08-08**. **On 2026-08-09 it flips to `overdue` while the checklist row stays `live` forever.** A §55 defect with a **known trigger date**. **Phase 4 is the fix.**
- **Do NOT "fix" it by setting liveness `null`** — that is the exemption machinery, and a federal annual return is a real filing with a real deadline. `null` would say *"no tier applies"*, drop the row from the verdict counters, and be wrong **always** rather than only inside the overdue window.

**★★ THAT OPEN QUESTION IS NOW CLOSED, AND IT CLOSES THE OTHER WAY — see §9g. The percentage IS rendered; Dom was right about Complétude and wrong about the product.** The paragraph below is kept as the original framing rather than rewritten.

**★ ONE OPEN QUESTION FROM THE CAMERA, unverified:** Dom reports **no surface on Complétude showing the completeness percentage.** `f1d0949` deleted the dashboard card that displayed it. **If nothing replaced it, `requirementsWeightedNum` and the percentage are internal metrics with no user-facing display — and four ships have been gated on a number nobody can see.** Not necessarily wrong (it feeds the verdict and the counters), but **confirm before gating on it again.**

## 9g. ★★ PHASE 2 — SHIPPED `fefb25a` (2026-07-29). INERT. What it added, what it did NOT, and what it found.

**Five fields on `FilingRule`** (`frameworks`, `exposure`, `actionKind`, `titleKey`, `suppressWhenSatisfied`) **+ `FilingDueCtx.fiscalYears`** (the phase 3 seam) **+ a hand-maintained `TitleKey` union + three `obligationTitle.*` keys per locale.** 275 insertions, **zero deletions**. Every locked rule is recorded in ZK_Core ("The registry's generic-emission fields"); this section records only what the PLAN got wrong.

**★★ THE PLAN'S OWN FIELD LIST WAS BUILT FROM THE FEEDER'S BLOCKS, NOT FROM THE REGISTRY'S ENTRIES — AND THOSE SETS DO NOT COINCIDE.** §3e item 3 says *"titles — both locales inline in the feeder"*, implying four pairs to move. There ARE four pairs, but **one belongs to `annual_meeting` (deferred) and the fourth registry rule, `qc_req_roster_update`, HAS NO TITLE LITERAL AT ALL.** It is not in `deadlines.ts`, which is calendar-only and says so in its own header; the roster REQ runs through `req-obligations.ts` and its display copy is `obligationNotice.req.title`, **YELLOW behind a `PENDING LAWYER GREEN` tripwire.** The build brief asked for a title that does not exist, and **CC declined to invent it — the tripwire doing exactly its job.** *Enumerate from the table being changed, not from the code being replaced.*

**★ THREE ITEMS OF §3e's SEVEN DID NOT SHIP AS FIELDS, AND THAT IS THE CORRECT OUTCOME:**
- **Upload-attach (item 5) — STRUCK, not a gap.** `requirementKey` derives from the rule's own `requirementKeys[0]`; `canUpload` derives from the **existing board-suppression derivation** — **NOT** from re-testing `cadence === 'anniversary'` a second time. Deriving one fact twice from one source is how `overlapMerge` and `boardSuppressCompletenessRows` came to exist (Lessons §56). Chain: `anniversary` → the completeness half is board-suppressed → the deadline row is the only thing left that can carry the affordance.
- **Multi-limb basis (item 6) — DEFERRED to phase 4** with `annual_meeting`, its only declarer. **A field with a type and zero declarers is a gate that cannot be proven** (Lessons §59), and its content is three Harvey-verified statutory limbs — a place to introduce a legal error silently.
- **Item 7 (`FilingDueCtx.fiscalYears`) — RESTORED.** The build brief's title, task list and count all said "six" and dropped it; **CC caught it against §3e's own text.** Without it phase 4 cannot emit per-year clocks — *the difference between the registry DESCRIBING obligations and DRIVING their emission.*

**★★ THE LOAD-BEARING GATE WAS NOT THE ARTIFACT, AND THIS IS THE TRANSFERABLE PART FOR PHASE 3.** Additive optional fields make Section D byte-identity **trivially true**, so it proves only that nothing was wired by accident. What carried the inertness claim: **`tsc` PROVEN TO REJECT** (deleting `exposure` from one entry fails `TS2741`, naming the property AND `FilingRule`, at that entry's own brace; restored by md5) plus **a content-diffed grep showing zero readers in `app/` or `components/`.** ⚠️ **The grep METHOD mattered:** a line-numbered diff **over-reported**, because a comment insertion shifted 45 lines of pre-existing content down and a naive diff reads that as new. **State the load-bearing gate explicitly in every remaining inert phase.**

**★ AND `annual_meeting` IS PHASE 4, NOT PHASE 2 — CC's recommendation, taken, for four reasons.** (1) **It cannot be inert** — the `isExternalRequirementKey` problem now carried by phase R. (2) Its limbs need Harvey, not a type. (3) The rename is its own unit of work. (4) ⚠️ **AN UNVERIFIED CONCERN, RECORDED AS ONE:** `noPriorAnnualMeetingRecorded` and the Wick guard may be ONE first-meeting predicate rather than two mechanisms — see §8 item 1. **All four are written into the code at that rule's bare `ruleKey` literal**, so the next reader meets them there rather than here.

## 9h. ★★ PHASE 3 — SHIPPED `74ae856` (2026-07-29). INERT. What it cost, and the gate that failed.

**`CompanyComplianceInput` gains `checklist` and `fiscalYears`, both REQUIRED, both unread.** Full detail in ZK_ShipLog and ZK_Core; this section records what the PLAN needs.

**★ PHASE 3 HAD NO HIDDEN COST — the fact the investigation was for.** Exactly ONE construction site and ONE consumer, and the site already held the whole result object: `completeness.checklist` was read six times there, `completeness.fiscalYears` was in scope and unused. **Two property accesses. No fetch, no signature threading, no second caller.**

**★ THE GATE CAME FREE BECAUSE THE FIELDS ARE REQUIRED.** `tsc` fails between the interface edit and the construction-site edit — a natural rejection proof rather than a synthetic break — and it produced a **SINGLE** error, independently corroborating that one construction site exists.

**★ `ctx.fiscalYears` IS DELIBERATELY UNPOPULATED.** Phase 3 makes the seam REACHABLE; phase 4's loop fills it. Filling it early would hand a fiscal-year set to `qc_initial_declaration` — `cadence: 'once'`, no fiscal year in its logic — asserting a relationship that does not exist. ⚠️ **And `FilingDueCtx` is never constructed as a named value:** three inline object literals, one per rule, each carrying only its own anchor.

**★★ THE THREE NULL-FY ANSWERS, WRITTEN DOWN SO PHASE 4 INHERITS A LIST RATHER THAN REDISCOVERING IT:**
| rule | null-FY behaviour | why |
|---|---|---|
| `qc_req_annual_update` | **NO ROW** | art. 45 LPLE ties the update to a COMPLETED fiscal year; a due date before one closes is not early, it is fictional |
| `annual_meeting` | **NO ROW** | the meeting presents statements for a completed year; and a not-yet-due obligation must never render as lateness |
| `fed_annual_return` | **STILL FIRES** | anniversary-anchored, wholly independent of the fiscal year; only its ATTACH-KEY needed fixing |
`qc_initial_declaration` has no null-FY answer — it never consults `fyEnd`.

**★ A JUSTIFICATION ROTTED AND PURITY DID NOT.** Three docblocks claimed the feeder *"stays record-agnostic"*. It still performs no I/O, but it now HOLDS the records. **The property survived; the reason given for it did not, and nothing type-checks a reason.** Corrected once, where the claim originated — three copies of one correction rot exactly as two versions of one fact do.

**★★ AND THE BRIEFED GATE FAILED, WHICH IS WHY §7 IS REWRITTEN.** Section D moved against `item4-before2/` on a phase that could not have moved it. CC refused *"my diff cannot do that"* as evidence — **"cannot is an argument, and the gate needs a measurement"** — and isolated code from data with a detached worktree at HEAD: **HEAD alone and HEAD+phase-3 are BYTE-IDENTICAL.** The movement was DATA: two ACME `director_mandate` events lost their generated documents, re-ranking 118 rows around the hole. **Third baseline death in three days. See §7.**

## 9i. ★★ PHASE R-1 — SHIPPED `111c21c` (2026-07-30). What it proved, what it did NOT, and the blocker it removed.

**The semantic half of phase R, split out on a measurement:** semantic change = **1 file, 2 call sites**; rename = **9 importers by path, 6 by symbol**. Bundled, R-1's proof drowns in a nine-file mechanical diff and R-2's byte-identity gate gets credited to R-1. Split, each half has a gate that can fail for the right reason.

**★ MAX'S ORDERING REASON WAS WRONG AND CC CORRECTED IT.** Max argued R-1 first because R-2-later leaves a *lying name*. It does not — after R-1 the registry still contains only external entries, so `FILING_REGISTRY` remains accurate. **The name only starts lying when `annual_meeting` joins, which is phase 4.** R-1 and R-2 are independent; **the only hard constraint is that both land before phase 4.** The order stands on diff hygiene alone.

**★★ THE GATE WAS A REJECTION PROOF — the shape every remaining inert phase should copy.** All four entries are `exposure: 'external'` AND `actionKind: 'file_externally'`, so the change is inert on live data **by construction** and Section D byte-identity is trivially true. Instead: one detached worktree, one synthetic `exposure: 'internal'` entry inserted by the **same script** both times, only the code differing. **RUN 1 (old) → `exposure=external` — the defect, OBSERVED. RUN 2 (new) → `exposure=internal`, `hasFiling` unchanged at `true`.** One field moved, one held — discrimination and field-independence in one artifact. ⚠️ **CC's first design established RUN 1 by READING the deleted source; Max amended it to MEASURE.** *"Cannot is an argument, and the gate needs a measurement."* Full method → ZK_Core / Lessons §75.

**★ THE CLAIM COVERS THREE ENTRIES, NOT FOUR.** `qc_req_roster_update` has **zero `requirementKeys`** (six docKeys instead), so `filingForRequirementKey` can never resolve to it — invisible to this path before and after. **A gate phrased "all four entries behave identically" is a claim about three.**

**★★ IT RESOLVED ONE OF `annual_meeting`'s FOUR DEFERRAL REASONS — the disqualifying one.** §9g's reason (1) — *"it cannot be inert, because `isExternalRequirementKey` would report the annual meeting as external"* — is gone with the predicate. Marked ✔ RESOLVED **in place, numbering kept**, because this document carries the same four under the same numbers and renumbering would desynchronise the code and the plan across a boundary neither party can see across. **Three remain, and the real one is (2): the three Harvey-verified statutory limbs.** **D-B's structural cost is now paid.**

**★★ AND THE THING THE PHASE NAME IMPLIES BUT DOES NOT DELIVER — say it in the phase-4 brief rather than letting it be rediscovered: the registry is NOT the single source of truth for `exposure`. FOUR sites assert it; R-1 converts ONE.** `deadlines.ts` (3× external, 1× internal) and `events.ts` (2×) still pass hardcoded literals at their push sites, and `FilingRule.exposure` is read by nothing else. **⚠️ ONE OF THEM MUST NEVER COLLAPSE:** `events.ts`'s Stage-1 `'internal'` is deliberate — Stage 1 is the *document* half, and pointing it at the rule's `'external'` would raise its score ~67% and silently reorder the board. **EXPOSURE IS A PROPERTY OF THE ROW, NOT ALWAYS OF THE RULE**, and the split follows `cadence` exactly as `titleKey`'s does.

**★ A CLAIM THIS DOCUMENT ALSO CARRIED, now corrected in code:** `filing-registry.ts`'s *"adding a future government requirement is ONE entry here — no edits elsewhere"* was **false TODAY**, not merely narrowed by D-B — §3a's own measurement (nothing iterates the table to emit) is why. It now states the target as a target. **Two of this plan's own paragraphs quote the falsified sentence**; they are historical narration of what the code said and are left standing as such.

## 9j. ★★ PHASE R-2 — SHIPPED `41158de` (2026-07-30). The rename, and the census defect it exposed.

**9 symbols + the file, `git mv` so history follows** — `--follow` returns three commits *through* the rename, reaching R-1 and phase 3. **`filingFor*` → `ruleFor*`, not `obligationFor*`** (CC's counter-proposal, taken): these return an `ObligationRule`, and `Obligation` is a live heavily-used type in the same folder. The payoff showed in a file CC had not examined when proposing it — `rank.ts` now reads `const rule = ruleForObligation(o)`.

**★★ TWO NATURAL REJECTIONS, and they are why the stages were split.** Symbols first, file second, so each fails for its own reason instead of one drowning the other. **STAGE A: 6 files, 16 errors. STAGE B: 9 files, 9× TS2307.** The 6/9 gap **independently corroborates the importer split** — three importers reference only the path and were invisible to Stage A. A bundled rename could not have shown this. ⚠️ **Stage A is `TS2305`/`TS2724`, NOT `TS2304`** — all six use *named imports*, so `tsc` fails at the import statement and never reaches a usage site. **A future brief predicting TS2304 would read a correct result as a discrepancy.**

**★★ TWO SYMBOLS ESCAPED EVERY CENSUS, and the reason is this plan's own recurring defect.** A **private helper in a consumer file** (`filingRuleForObligation` in `rank.ts` — never an export, so never on a list drawn from exports) and **the Section D harness OUTSIDE THE REPO** (`Max - CTO/baselines/probe-consolidation.ts`, a *tenth* importer; the byte-identity gate crashed mid-run until a throwaway copy was patched). **Both were found by a post-rename residual sweep, not by any list.** CC: *"the symbol list was drawn from the module's EXPORTS; the actual blast radius was every place the NAME appeared."* → Lessons §76.

⚠️ **THE CANONICAL HARNESS IS STILL BROKEN AGAINST `main` UNLESS SEPARATELY REPAIRED** — it is executable, not in git, and the next Section D capture fails without it.

**★ BYTE-IDENTITY WAS A REAL GATE HERE, unlike R-1's** — a rename must not move output, so the test could fail for the right reason. 327 rows, md5 identical. *Caveat recorded: RUN B used an identifier-patched harness, five substitutions that cannot alter printed output.*

**★ AND ONE FINDING THAT OUTLIVES THE PHASE:** `obligationFiscalYear`'s docblock was **TRUE and its name was WRONG** — an accurate sentence about one use case does not license a name that generalises to all of them. **Widened, not deleted**; the receipt survives as the example. Same discipline applied to the registry header's drift-sites history, which still reads "filing" **because every entry was one when it was written.**

## 9k. ★★ PHASE 4a — SHIPPED `c587441` (2026-07-30). Genericity only, and two gates neither of which is sufficient alone.

**Three hand-written blocks -> one pass over `OBLIGATION_REGISTRY` reading declared fields.** Each rule still emits **at most one row**; iteration is 4b, `annual_meeting` is 4c and its block is untouched below the loop.

**★★ THE PHASE SPLIT WAS CC's, NOT THIS PLAN's.** Max proposed 4a/4b along *which rules*. CC measured the seam differently and was right: **the real division is WHICH BEHAVIOUR** — genericity (inert), iteration (moves), `annual_meeting` (new rows). **The row COUNT alone distinguishes all three**, so bundled they would have produced one undifferentiated diff. Every genericity gap found was a **block-4** gap; blocks 1-3 needed **no new field at all**.

**★★ TWO GATES, AND THE SHAPE IS THE REUSABLE PART.** Byte-identity proves the real rules did not move — but **cannot distinguish a generic loop from a switch on three hardcoded ruleKeys.** A rejection proof distinguishes them — and cannot prove inertness. Results: **byte-identical whole-file, 327 rows**; and a **synthetic fifth entry** differing on `exposure`, `actionKind`, cadence and both omit-branches emitted a correct row where HEAD emits nothing — **11/11 predicted fields, prediction written BEFORE the run**, with an **N=2 positive control** separating *"HEAD is not generic"* from *"the probe never ran."*

**★ THE GUARDS WERE ALREADY DECLARED — PHASE 2 DID MORE THAN THIS PLAN CREDITED.** Three of four blocks' guards were expressible with **no new field**, because of a convention this plan never named: **`dueDate` returns `Date | null`, so "the anchor this rule needs is absent" is ALREADY EXPRESSED by the date rule returning null.** With `frameworks` and `suppressWhenSatisfied`, blocks 1-3 needed nothing new.

**★★ AND THE PRE-INCORPORATION RISK IS SMALLER THAN §6 AND §8 CLAIM. `0f7deee` CANNOT RECUR BY CONSTRUCTION** — `computeDefaultActiveYears` is **incorporation-capped**, so a loop over `fiscalYears` cannot produce a pre-incorporation row. Phase 3 sourcing that set from the completeness result de-risked it before phase 4 began. What remains splits in two and **only one half is blind to the camera**: the **open-year** risk affects every company including both fixtures and WILL show in Section D; only the **`fyEnd === null`** path needs a first-year company, which no fixture can be. ★ **AND THE NULL PRODUCES TWO BEHAVIOURS, NOT THREE** — three labelled answers, but (a) and (b) are both *no row* for different legal reasons. Cadence already encodes the two modes; that correspondence is **asserted at runtime** because it rests on **N=1**.

**★ WHAT THE PHASE DOES NOT DO, and 4b's brief should say so:** `exposure` is still asserted at **four** sites; 4a converts none beyond R-1's one. ⚠️ And **`events.ts`'s Stage-1 `'internal'` must NEVER collapse into the registry** — exposure is a property of the **ROW**, not always of the **RULE**.

## 9l. ★★★ LA PHASE 4b EST MORTE TELLE QU'ÉCRITE — LIRE AVANT DE LA SCOPER (2026-07-31)

**Ce plan décrit 4b comme : « les six lignes REQ historiques d'Acme gagnent des horloges ». LA RÈGLE DE DOM DIT L'INVERSE.**

**★ LA RÈGLE, verrouillée : UNE LIGNE DOIT ÊTRE ACTIONNABLE MAINTENANT, MÊME SI ELLE N'EST PAS URGENTE.** *« Le board, c'est "Que faire maintenant". »* Les années passées sont **estompées, téléversables, hors dénominateur, jamais au board.**

**Donc le travail n'est plus d'ajouter des échéances — c'est d'ENLEVER des lignes**, et elles sont au board aujourd'hui (mesuré : 2019-2024 sur Acme, `due=null`, toutes dans le flux classé).

**★★ ET HARVEY CONFIRME QUE C'EST UN FAIT JURIDIQUE, PAS UN CHOIX PRODUIT** (2026-07-24, VERT) : l'obligation de 2019 **reste due** (art. 45, aucune guérison rétroactive) **mais toutes ses conséquences sont éteintes** — prescription pénale à 5 ans (art. 163.1), radiation dépassée (art. 59). *« Il n'existe aucune action légale utile ni requise à poser. »* **Donner des horloges à ces lignes les placerait EN HAUT du board** — `overdue` devance tout — **pour des obligations qu'on ne peut pas discharger.**

⚠️ **NE PAS HÉRITER DES CHIFFRES DE §6 ET §8.** *« 56 lignes gagnent des horloges »* et *« ~+50 »* ont été dérivés **avant** la règle, en comptant les lignes `due=null` par bloc — donc ils **incluent l'exercice OUVERT** que la règle exclut. **Ce sont des bornes supérieures. Re-mesurer.**

**★ ET L'ITÉRATION PAR ANNÉE N'EST PEUT-ÊTRE PLUS NÉCESSAIRE DU TOUT.** Sous la règle, le feeder d'échéances émet **déjà la bonne chose** : une ligne, l'occurrence courante. **À vérifier avant de scoper quoi que ce soit** — 4b pourrait se réduire à retirer des lignes du board et changer le rendu sur Complétude.

**★★ ET LE VRAI DÉFAUT MESURÉ N'EST NI L'UN NI L'AUTRE :** board et Complétude **se contredisent sur les lignes fondationnelles** — 9 sur 9 chez Wick, qui est vide, **donc la forme de chaque client au premier jour.** Une décision livrée (`9a5c755`, `exempt_from_lateness`) **n'atteint qu'un des deux chemins**. Détail et méthode → ZK_Queue.

**★ ET LE CYCLE DE VIE COMPLET D'UNE OBLIGATION REQ dépasse ce que le registre peut exprimer** — fenêtre de 60 j (art. 73), escalade à la 2ᵉ échéance manquée (état de la SOCIÉTÉ), délai conditionnel selon le régime de dépôt. Les cinq lacunes sont énumérées et **gardées par machine** dans `docs/registry-export.md` §4 — **régénérer avant de s'en servir** (`npm run export:registry`).

## 9m. ★★ LE MODÈLE DES DEUX FAMILLES PRÉCÈDE TOUT LE RESTE DE L'ARC (2026-08-01)

**Ce plan raisonne sur `liveness` comme si toutes les obligations étaient de même nature. Elles ne le sont pas.**

**DATÉE** — on connaît la règle et le délai. Mot : *« en retard »*. Échéance par ligne. Plusieurs états.
**DURÉE NEUTRE** — Harvey VERT : la durée est **légalement neutre**, donc le retard **n'existe pas**. Mot : *« pas à jour »*. **Un seul état.** Déclencheur : **le cap**, une date par SOCIÉTÉ (premier exercice clos + 6 mois).

**★ CE QUE ÇA CHANGE POUR L'ARC :** l'axe GRAVITÉ que §9k réclamait **n'est plus nécessaire**. Il était réclamé parce que *« en retard »* couvrait trop — fenêtre de 60 j, exposition pénale, exposition à la radiation, ET documents constitutifs, sous un seul mot. **La séparation retire les constitutifs de cet axe entièrement**, et ce qui reste est homogène : des obligations avec des dates.

**★ ET `exempt_from_lateness` PREND SON VRAI SENS :** ce n'est pas *« cette ligne est dispensée »*, c'est **le marqueur de la NATURE de l'obligation**. `true` = durée neutre → cap. `false` = datée → échéance.

⚠️ **CONSÉQUENCE POUR 4b (§9l) :** rien ne se scope tant que l'état *« pas à jour »* n'existe pas dans le verdict. **Il doit être une valeur de LIVENESS, pas un décompte** — Core interdit une quatrième case fondée sur un compteur, et Boundary B tient sur *« en règle = rien à faire aujourd'hui »*.

## 9n. ★★ LE CLASSEMENT — ✅ RÉSOLU `968a7ae` (2026-08-11). CETTE SECTION EST RÉÉCRITE : SA VERSION v14 ÉTAIT FAUSSE.

> ⚠️⚠️ **CE QUE CETTE SECTION DISAIT ET QUI ÉTAIT FAUX :** *« la cause mesurée : `VIRTUAL_URGENCY_FLOOR = 0.55` »*, et *« le geste est une passe de réglage »*. **La constante produisait bien le facteur 3,3 — mais elle n'était pas ce qui bloquait.**

**LE FAIT QUI TRANCHE, mesuré sur le disque au HEAD :** `LIVENESS_RANK` est un **seau ABSOLU** — `rank.ts:243-248` compare le seau **avant** toute comparaison de score, avec **retour immédiat**. **Le score n'est jamais consulté entre seaux.**

**→ Aucune valeur de constante ne peut faire passer une ligne `live` sous une ligne `regularize`. La passe de réglage était structurellement incapable de fonctionner** — et elle était prescrite ici, dans ZK_Queue **et** dans ZK_Core. *Trois documents, une seule hypothèse jamais confrontée à l'instrument.* → ZK_Lessons §86.

### Les trois pistes réellement éliminées par la mesure

| piste | pourquoi elle échoue |
|---|---|
| **une passe de réglage** | le seau est absolu (ci-dessus) |
| **un 4ᵉ membre de `ObligationLiveness`** | recensement depuis les occurrences réelles : **11 consommateurs — 2 cassent au typecheck, 7 IGNORENT EN SILENCE**, dont 4 qui classeraient le nouveau membre en `overdueProlonged`, le pire état du produit, pour une obligation sans conséquence. Plus **`FilterKey`**, union parallèle recopiée à la main, verte pour toujours. |
| **retirer le classement `foundational`** | **`rank.ts` ne lit jamais `category`** (`isFoundational` = `source` ET `year`). Et le changer en base enverrait la ligne dans la boucle annuelle → **8 lignes RE-200 pour Wick**, le défaut que la phase 1 avait corrigé. |

*(Les trois pistes de v13/v14 — bucket seul, score seul, câbler `exempt_from_lateness` — restaient vraies de leur époque mais ne décrivent plus le problème. `exempt_from_lateness` reste mesuré comme traversant la frontière des deux familles, 5 true / 3 false.)*

### Ce qui a marché

**Le cap arrive AU SITE D'APPEL du feeder, jamais dans `liveness.ts:74`** — cette branche est **PARTAGÉE** par les deux chemins ; le chemin checklist l'écrase immédiatement (`requirement-completeness.ts:322`). **La discrimination n'est pas un drapeau ajouté : c'est une séparation qui existe déjà.**

**Mesuré par expérience :** une sonde qui a changé la valeur des 8 lignes de Wick côté board **n'a déplacé aucun des 4 compteurs du verdict** (10/8/20). **Les deux chemins sont disjoints. 3 sites touchés, l'union ne bouge pas.**

**★★ ET LE PIÈGE QUI A FAILLI FAIRE ÉCHOUER LE CORRECTIF EN L'ACCUSANT :** `completedFiscalYearEnd` retourne **le plus récent** exercice clos, pas **le premier**. L'utiliser aurait donné à Wick un cap **dans le futur** — fondationnelles restées `live`, board immobile, mesure accusant la règle. **Et Café du Coin ne pouvait pas le révéler** (jeune : premier = plus récent). **C'est Wick qui l'attrape.** → §87.

---

## 9o. ★★ OÙ EN EST L'ARC AU 2026-08-11, ET CE QUI RESTE (lire en premier)

**Livré :** phases 1, 2, 3, R-1, R-2, 4a — plus `5adc6cd` (doublon de la déclaration initiale), `18558b6` (le fédéral peut être en retard) et **`968a7ae` (le cap)**.

### ★★★ L'ITEM ACTIF — LES TROIS SURFACES NE DISENT PAS LA MÊME CHOSE

**Le board est corrigé. Complétude et le verdict ne le sont pas.** Sur **Fixture Cap** (CBCA, 5 mois, zéro document), constaté sur capture :

| surface | aujourd'hui | visé |
|---|---|---|
| Board | ✅ cinq fondateurs calmes | inchangé |
| Verdict | ❌ « À régulariser · **4** » | **« En règle · 0 »** |
| Complétude, filtre « À régulariser » | ❌ 4 lignes, dont **3 sans aucun délai** | **vide** |

**Cause :** le cap n'atteint que le chemin **board**. Complétude et le verdict tournent sur le **plancher fondationnel**, qui n'a **aucune notion de date**.

**Les quatre gestes, dans l'ordre — détail dans ZK_Queue A3 :**
1. **Retirer `cbca_declaration_initiale_qc` du modèle fédéral** — 🟢 l'art. 38 LPLE **ne s'applique pas** aux sociétés fédérales (art. 30). **Retrait, pas reclassement.**
2. **Migrer les six clés d'organisation à `exempt_from_lateness = true`** — SQL, aucun code.
3. **Le cap atteint le chemin checklist.**
4. **Retirer le plancher fondationnel** — ⚠️ commit séparé, **gaté** sur la preuve que (3) couvre Boundary B.

**★ Le test, et il est de Dom :** *si je filtre « À régulariser » dans Complétude, est-ce que ces items le sont ?*

### ★★ LA PHASE 4b DEVIENT PORTEUSE

**Harvey, 2026-08-11 🟢 :** l'exposition fédérale à la **dissolution ne se prescrit pas** — elle dure tant que dure l'état de défaut. *« Ce n'est pas la prescription qui gouverne l'affichage, c'est l'état de défaut. »*

**→ Chaque année non produite bloque la sortie de l'état de défaut.** Wick doit **7 rapports**, le produit en affiche **un**. La multiplicité manque à **cinq endroits** (signature de `dueDate`, boucle une-ligne-par-règle, `year` dérivé, ligne catalogue unique, année d'attache de `suppressWhenSatisfied`).

**★ Deux logiques opposées, mêmes faits :** au **Québec**, une année ancienne d'une société aujourd'hui à jour n'expose plus à rien (art. 59 exige deux **consécutives**). Au **fédéral**, elle **empêche d'être à jour**.

⚠️ **4b reste morte telle qu'écrite en §9l** — elle disait « les lignes historiques gagnent des horloges », la règle de Dom dit qu'elles **quittent** le board. Les chiffres 56 et ~+50 sont des **bornes supérieures** à re-mesurer.

### ⚠️ CE QUI N'EST PLUS VRAI DANS CE PLAN

- ~~« le 2026-10-08, la ligne fédérale de Wick descendra du rang 9 au rang 17 »~~ — **caduc.** La prédiction portait sur un ordre que le cap a remplacé. *À re-mesurer si on veut la question.*
- ~~« la cause mesurée est `VIRTUAL_URGENCY_FLOOR` »~~ — voir §9n. **La constante est déclassée** : elle n'ordonne plus que des lignes **sous le top-5**, et le board ne défile pas (D4).
- ~~« le classement est le seul item actif »~~ — remplacé par **la cohérence des trois surfaces**.

### ★ LES FIXTURES SONT QUATRE

| | régime | constituée | FYE | cap |
|---|---|---|---|---|
| **Acme** | LSAQ | 2018-04-17 | 31 déc | échu · **0 fondationnelle classée (0/36)** |
| **Wick** | CBCA | 2018-08-08 | **31 mai** | 2019-11-30, échu · **zéro document** |
| **Café du Coin** | CBCA | 2025-06-19 | 31 déc | 2026-06-30, **échu de 42 j** |
| **Fixture Cap** | CBCA | 2026-03-02 | 31 déc | 2027-06-30, **NON échu** |

⚠️ **`PROBE_FROZEN_DATE` ne déplace que l'horloge AMBIANTE**, pas les horloges INJECTÉES de `CLOCKS`. **Le cap se compare contre le `today` du FEEDER.** Et les captures ont désormais **cinq** blocs d'horloge contre quatre avant — toute comparaison croisée sortira `DIFFERE` pour rien.

---

## 9p. ★★★ (F) INVESTIGUÉ — CE QUE LA MESURE RENVERSE (2026-08-12, lecture seule au HEAD `6f0a48c`)

> **Dom a mis cet item en priorité 1 :** *« Notre produit va être jugé à notre capacité à aider l'utilisateur. Et F bien fait démontre cette capacité. »*

### ★★ TROIS DÉFAUTS, ET LE PRODUIT SE TROMPE DANS LES DEUX SENS

**(1) SUBSTITUTION — le produit RASSURE une société qui risque la dissolution.** Wick doit **HUIT** rapports fédéraux : **sept en retard** (2019-2025, jusqu'à 2 501 jours) et un à venir. **Le board affiche exactement le huitième, pas encore dû, et tait les sept autres.** *Ce n'est pas un sous-comptage.* 🟢 L'exposition fédérale ne se prescrit pas ; **et c'est le seul item qui contamine l'export de due diligence.**

**(2) BRUIT — six lignes REQ sans échéance ni action utile** (Harvey : *« aucune action légale utile ni requise »*), qui noient la ligne courante. ⚠️ **Sauf la plus récente** : fenêtre de l'art. 52.

**(3) FAUX POSITIF — Fixture Cap, 5 mois, « en retard de 102 jours »** sur un rapport qu'elle ne doit pas. `dueDate` lit `incorporationDate` pour le mois et le jour, **jamais pour l'année**, qui vient de `today`.

### ⚠️⚠️ ET CORRIGER (3) FAIT JETER L'ASSERTION — MESURÉ EN WORKTREE JETABLE

`HARNESS_EXIT = 1`. L'assertion `deadlines.ts:506-518` :

> `'fed_annual_return' (cadence 'anniversary') returns null when no fiscal year has closed […] **the rule needs a DECLARED mode field — a gap A4 phase 4 deferred on the evidence that cadence encoded it.**`

**Elle jette DANS `deadlineObligations` (:509), pas au chargement.** → **la page ne rend pas pour les sociétés sans exercice clos** — invisible sur les trois fixtures anciennes, **frappe tout nouveau client**.
★ **Aujourd'hui personne ne le voit parce que le produit MENT au lieu de planter.**

### ★★ LE PARI DE LA PHASE 4 A PERDU

L'assertion fait respecter *« le MODE se déduit de la CADENCE »*. **Faux sur une règle vivante sur deux** — `fed_annual_return`. *Et son propre commentaire l'annonçait : « THAT CORRESPONDENCE RESTS ON ONE per-fiscal-year RULE — it may be construction or it may be coincidence. »*

**★ Le champ de mode doit être lu à DEUX endroits :** `deadlines.ts:507` **et** `check-obligations.ts:103` (**FACT 1**).
★ **La cadence n'est pas à remplacer** — seize lectures, six questions, elle répond juste à cinq. **Seul le MODE lui échappe.**

### LE DÉCOUPAGE — LES CINQ SITES NE SONT PAS UN BLOC

| geste | mode ? | sites |
|---|---|---|
| **(i) faux positif** | OUI | `:507` + `check-obligations:103` |
| **(iii) ajout fédéral** | OUI | + 1 (signature), 3 (`year`), 5 (`attachYear`) |
| **(ii) retrait québécois** | **NON** | site 4 seulement |

**★★ QUÉBEC = RETRAIT (complétude) · FÉDÉRAL = AJOUT (échéance). Aucun site partagé.**
★ **Le Québec n'a RIEN à itérer** — le feeder émet déjà l'occurrence courante. *§9l posait la question ; la réponse est oui.*
⚠️ **Le site 4 est mal situé dans §6/§8** : pour le fédéral, le catalogue ne contribue **rien** au board (*board-suppressed*).

### ⚠️ LES CHIFFRES DE §6/§8 SONT MORTS — VOICI LES VRAIS
~~56 lignes~~ · ~~~+50~~ → **Wick 43 → 37** (38 avec l'art. 52) · **Acme 36 → 30** (31) · **Café du Coin : aucun effet.**
⚠️ **Et le dénominateur bouge de SIX si le retrait passe par le site 4 : Acme 48,7 % → 69,7 % sans avoir rien déposé.** *Board-seulement, il ne bouge pas.* **DÉCISION PRODUIT.**

### ⚠️⚠️ L'ART. 52 N'EST PAS CALCULABLE
**Zéro occurrence de « période » dans du code.** *« La période de déclaration »* n'est **pas** « fin d'exercice + 6 mois » — c'est une fenêtre du **registraire**. ★ **Même forme que la voie liste+avis : une notion juridique absente du modèle.** Le compte à rebours est **une donnée à acquérir, pas un calcul à écrire.**

### ⚠️ ET LA COMPARAISON PROPOSÉE EST TROP ÉTROITE
`inc.getFullYear() >= ctx.today.getFullYear()` compare des **années civiles** ; l'obligation naît à **l'anniversaire**. *Société du 2026-11-15 mesurée le 2027-01-10 → faux positif d'un mois et demi.*

---

## 9q. ★★★ L'ASSERTION CADENCE↔MODE — INVESTIGUÉE, ET SON PROPRE CORRECTIF EST IMPOSSIBLE (2026-08-12)

> ⚠️ **§9p reprenait la phrase de l'assertion** — *« the rule needs a DECLARED mode field »* — **comme si elle nommait la solution. Trois formes mesurées en worktree jetable : elle ne la nomme pas.**

### ✗ (A) LE CHAMP BINAIRE EST MORT PAR LA FORME

| | `tsc` | `check` | harnais |
|---|---|---|---|
| **A-true** | 0 | 0 | **1 — JETTE sur Fixture Cap** |
| **A-false** | 0 | **1** | 0 |

**Aucune valeur ne passe les deux portes.** Avec le correctif, `dueDate` **n'a pas de valeur binaire correcte** : sans exercice clos elle rend `null` l'année de constitution et **une date sinon**. ⚠️ Et `tsc` force la règle `event` — sautée avant l'assertion — à répondre (`TS2741`). **Champ mal placé.**

### ★★ FACT 1 A DEUX MOITIÉS, ET LA SECONDE EST UN INTERDIT

**A** — *une règle `per-fiscal-year` doit rendre `null` sans ancre* : **vraie, vivante**, mutation rejetée et nommée.
**B** — *toute autre cadence non-event doit rendre une date* : **falsifiée par le correctif**, et ★ **elle rejette le correctif LÉGITIME avec le message exact qu'elle réserve au bug.** *Un garde qui ne distingue pas les deux n'en garde aucun.*

### ✅ (C2) — L'ASSERTION À SENS UNIQUE + FACT 5

L'assertion garde **seulement la moitié A** (la classe fantôme, `0f7deee`, déjà shippée une fois). **FACT 5 est une table exécutable de la frontière** : même société, deux horloges, deux attendus.

| mutation | FACT 5 | FACT 1 | FACT 3 |
|---|---|---|---|
| correctif **absent** | ✗ *no row attendu, a row obtenu* | ✓ | ✓ |
| correctif **trop large** | ✗ *a row attendu, no row obtenu* | ✓ | ✓ |

★ **Preuve indépendante :** sous la mutation trop large, **Café du Coin (inc 2025) perd sa ligne, Wick (inc 2018) la garde.**

### ★★ CE QUI DÉCIDE — (C) EST LA SEULE QUI GRANDIT AVEC 4b
**(A)** bloque la multiplicité (un booléen ne décrit pas un tableau de dates) · **(B)** ne casse pas mais ne protège rien · **(C)** le throw s'exécute N fois, forme inchangée, **et FACT 5 affirme un ENSEMBLE de lignes — le critère d'acceptation que 4b n'avait pas.**

### ⚠️ NON MESURÉ, NOMMÉ
**La forme structurelle pour `imm=N`** — un **résultat discriminé** (*une date · pas d'instance · ancre absente*) porté par le TYPE plutôt que par deux coïncidences. **Peut-être la vraie forme. Mesure suivante, indépendante.**

---

## 9r. ✅ (C2) LIVRÉ — `19d86b8` (2026-08-12). Ce que le geste (i) a fermé, et ce qu'il laisse.

**Trois fichiers, +175/−41.** Le faux positif de l'année de constitution (Fixture Cap, *« en retard de 102 jours »*, score **1.0000**) est corrigé, **et l'assertion qui aurait fait planter le dashboard est partie dans le même commit.**

### ★ CE QUI A ÉTÉ MESURÉ, PAS ARGUMENTÉ
- **(A) le champ binaire est mort par la FORME** — A-true : CI verte / production jette · A-false : CI rouge / production verte. **Pas de troisième valeur.** Et `tsc` forçait la règle `event`, sautée avant l'assertion, à répondre (`TS2741`).
- **FACT 1 avait DEUX moitiés.** La moitié B *« toute autre cadence doit rendre une date »* **rejetait le correctif LÉGITIME avec le message exact réservé au bug.** ★ **Un garde qui ne distingue pas un correctif d'un bug n'en garde aucun — il interdit.** Retirée aux deux sites, sur preuve.
- **FACT 5 est le seul garde qui voyait le défaut :** fix retiré → FACT 1 ✓ FACT 2 ✓ FACT 3 ✓ FACT 4 ✓, **FACT 5 ✗**.
- **Prouvé dans les deux sens** : fix absent → ✗ limbe 1 · fix trop large → ✗ limbe 2. ★ **Et le dommage collatéral du trop-large mesuré SUR LE BOARD** : Café du Coin (inc 2025) perd sa ligne, Wick (inc 2018) la garde.
- **Harnais, quatre fixtures, comparaison PAR ID avec contrôle positif** (baseline contre elle-même : 20 blocs, 585 ids, 0 écart) : **exactement 2 lignes retirées, les deux sur Fixture Cap aux horloges 2026. Rien d'ajouté.** ★ **La ligne est inchangée aux horloges 2027-2029 — le correctif retire l'année de constitution, pas la règle.**

### ★★ POURQUOI FACT 5 COMPTE POUR LA SUITE DE L'ARC
Il affirme un **ENSEMBLE DE LIGNES**, jamais une correspondance. **C'est exactement le critère d'acceptation dont (iii) l'ajout fédéral a besoin** — *année de constitution → 0 · année+1 → 1 · horloge 2033 → 7*. **Le garde grandit avec la multiplicité au lieu de la bloquer.**
★ **Et son sujet est de première année PAR CONSTRUCTION** — horloge dérivée de sa propre année de constitution. *Aucun futur édit d'un littéral de date ne peut le vieillir en silence : c'est le piège qui a rendu FACT 1 et FACT 3 aveugles.*

### ⚠️ CE QUE (i) NE FERME PAS
**(ii) le retrait québécois** et **(iii) l'ajout fédéral** restent entiers — voir §9p. Wick doit toujours **HUIT** rapports et le produit en affiche **un**.
Plus trois items ouverts : **la comparaison années-civiles vs anniversaire** (six semaines de sur-service, FACT 5 y est invariant) · **la colonne `imm=N`** tenue par deux faits de données · **★ la forme structurelle jamais mesurée** — faire porter au retour de `dueDate` la RAISON de son silence plutôt qu'un `null` ambigu.

---

## 9s. ✅ (E)/(F1) LIVRÉ — `12ebd51` (2026-08-13). Et le lot qui suit est la CONSÉQUENCE.

**Sur ACME, une cessation en retard de 47 jours était au rang 6, derrière deux émissions d'actions sans aucune échéance.** ★ *La date était à l'écran depuis le début — le marqueur la rendait — et le ranker était la seule chose à ne pas la lire.*

### ★ UN CHAMP, DEUX RESPONSABILITÉS
`daysUntilDue` est le **SEUL** champ d'horloge que `rank.ts` consomme. L'étape 1 le codait à `null` pour que la pastille suive le DOCUMENT et non le dépôt — **raison valide, toujours honorée.** Mais le même champ portait **l'urgence au classement**, et la nuller la perdait. *`null` = plancher · dépassé = `URGENCY_MAX`, le plus grand écart de la fonction, jamais déclenché pour ces lignes.*

**Les deux usages étaient DÉJÀ indépendants au site d'appel** — `deriveStatus` reçoit un `null` **littéral** en paramètre, il ne lit pas le champ. **0 pastille, 0 seau, quatre fixtures, par id.**

### ⚠️⚠️ LA RAISON POUR LAQUELLE LE SEAU NE BOUGE PAS — la formulation évidente est FAUSSE
Ce n'est **pas** que la liveness ignore l'horloge : **le seau d'un acte de rôle EN EST DÉRIVÉ, en amont** (`event-completeness.ts`). Ce feeder nullait le champ **en aval**, après coup. **(F1) RÉTABLIT UN ACCORD QUI EXISTAIT DÉJÀ.**
★ *Max avait consigné la cause inverse ; CC l'a démentie sur le disque avant d'éditer.*

### ★★ FACT 6 — ET IL A ÉCHOUÉ AVANT DE PASSER, DEUX FOIS
**Rien ne gardait ce chemin** : `check:obligations` n'importait ni le feeder d'événements ni le ranker, et `tsc` ne peut pas aider.
**La première version donnait la MÊME date aux deux sondes et PASSAIT sous sa propre mutation** — barreau 5, `id.localeCompare`, `'d' < 's'`. ★ **Le défaut qu'il garde, reproduit dedans.** Corrigé dans le SUJET : l'acte sans horloge est deux ans plus vieux, **seule l'horloge peut le faire perdre**, et une précondition garde le piège.
**Prouvé dans les deux sens ; les six autres faits verts sous la mutation.**

### ★★★ LE LOT SUIVANT — LA CONSÉQUENCE COMME AXE

> **Dom, 2026-08-13 :** *« Je tiens à ce que j'ai mentionné le 5 juillet, mais il faut retravailler cette règle. **Éviter de prendre du retard si et uniquement si les items à haute conséquence n'y sont plus.** »*

**Le seau implémente la première moitié. LA CONDITION N'A JAMAIS ÉTÉ IMPLÉMENTÉE** — la conséquence n'existait pas comme axe le 5 juillet. **Elle existe depuis le 2026-08-11.**

**Sur ACME :** rang 3 = REQ en retard (**MOYENNE**) · rang 4 = **assemblée annuelle**, marquée « En retard », **AUCUNE sanction publique** 🟢 · rang 5 = art. 41 (**BAS**) · rangs 1-2 = émissions d'actions, **★ INCONNU** (le top 3 par voix n'est pas détectable).

⚠️⚠️ **LA PORTE : presque toute montée vers ÉLEVÉE passe par la MISE EN DEMEURE DE L'ART. 73, dont la date n'existe nulle part.** *Corroboré par deux sources écrites séparément.* **Ce qui est calculable est plus bas que l'intuition.**
★ **Et « INCONNU » n'est pas « AUCUNE »** — *« la question n'est pas quel niveau donner, mais comment le modèle dit qu'il ne SAIT pas : c'est une question de TYPE, pas de valeur. »*
⚠️ **La conséquence ESCALADE avec le temps** — un champ fixe ne peut pas l'exprimer. *C'est ce qui a tué le champ de mode binaire le 2026-08-12.*

---

## 9t. ⚠️★★★ LE PLAN D'ATTAQUE (2026-08-13) — L'ORDRE A ÉTÉ RENVERSÉ ET LA PRÉMISSE EST MESURÉE FAUSSE (corrigée 2026-08-14)

> ⚠️⚠️ **CETTE SECTION EST CONSERVÉE PARCE QU'ELLE EXPLIQUE POURQUOI ON A CRU UNE CHOSE FAUSSE. Ne la lisez pas comme le plan en vigueur.**
>
> **CE QU'ELLE AFFIRMAIT**, cité : *« `dueDate` recalcule l'anniversaire de l'année en cours à chaque appel… La ligne se répare toute seule, et n'est en retard que quelques semaines par an »* · *« 13 lignes fédérales dans le harnais, **zéro `d` négatif** — le niveau ÉLEVÉE n'a jamais été exercé dans la simulation de l'axe de conséquence. Il y était décoratif. »*
>
> ⚠️ **POURQUOI C'ÉTAIT FAUX — UN ARTEFACT DU CHOIX DES HORLOGES, PAS UNE PROPRIÉTÉ DU PRODUIT.** Les cinq horloges de `CLOCKS` tombaient TOUTES hors de la fenêtre où la ligne fédérale est négative — pour Wick, **2026-10-08 → 2026-12-31** — et **TROIS au 1ᵉʳ janvier**, le point le plus positif de l'année. [MESURÉ 2026-08-13, une fois l'horloge `2026-12-01` ajoutée : **Wick atteint `d=−55` au 1ᵉʳ décembre et `d=−85` au 31 décembre**.] Le négatif existe **85 jours par an, ~23 %**. Le harnais ne pouvait pas le voir.
>
> ★ **CE QUI RESTE VRAI**, pour ne pas sur-corriger : `dueDate` recalcule bien l'anniversaire de l'année en cours, donc **la ligne SE RÉPARE chaque 1ᵉʳ janvier** — `−85` le 31 décembre, `+279` le lendemain. Ce qui était faux, c'est la CONCLUSION : que le négatif n'existerait jamais.
>
> ★★ **CE QUI SURVIT DU MOTIF, ET PLUS FORT QUE L'ARGUMENT D'ORIGINE :** Wick doit **HUIT rapports fédéraux**, le produit en affiche **UN**, et c'est celui **qui n'est pas encore dû**. La substitution est un défaut à part entière ; elle n'a jamais eu besoin de l'argument sur ÉLEVÉE, qui était le maillon faible.
>
> **→ Lessons §105 :** un état à **zéro occurrence** a TROIS causes — *impossible* · *possible et non atteint* · *possible et **NON OBSERVABLE***. Les trois se lisent **identiquement** dans l'artefact. Ici c'était la troisième, et on a conclu la première.

### 1️⃣ (iii) L'AJOUT FÉDÉRAL — **le préalable, pas un lot parallèle**

**Wick doit HUIT rapports, sept en retard, le produit en affiche UN — celui qui n'est pas encore dû.**

**★★ LA MESURE QUI EN FAIT UN PRÉALABLE :** `dueDate` recalcule l'anniversaire **de l'année en cours** à chaque appel. Au 1ᵉʳ janvier 2027, l'échéance devient 2027-08-18 — **jamais 2026-08-18 échue.** La ligne **se répare toute seule**, et n'est en retard que quelques semaines par an.
⚠️ **Or le défaut fédéral persistant (§2d) est la SEULE conséquence ÉLEVÉE que le produit sache calculer.** *13 lignes fédérales dans le harnais, **zéro `d` négatif** — le niveau ÉLEVÉE n'a jamais été exercé dans la simulation de l'axe de conséquence. Il y était décoratif.*
★ **Donc sans (iii), l'axe de conséquence trie correctement AUCUNE, BAS et MOYENNE — et le niveau le plus grave n'apparaît presque jamais.**

**Quatre sites**, tous dans `deadlines.ts` et le contrat de règle. **Le catalogue ne contribue rien** → dénominateur intact, Complétude non touchée.
⚠️ **Non résolu :** le dégagement individuel — un seul reçu dégagerait la mauvaise année.

### 2️⃣ L'AXE DE CONSÉQUENCE — investigation complète (§9u ci-dessous n'existe pas ; voir ZK_Queue A3)

### 3️⃣ (ii) LE RETRAIT QUÉBÉCOIS + L'ALERTE ART. 52 — ✅ **DÉBLOQUÉ**

**Harvey, tour 4 :** les bornes de la période se dérivent **entièrement** de la fin d'année d'imposition. Période **lendemain de la fin d'exercice → + 6 mois** · **fenêtre de guérison → jusqu'à la fin d'exercice suivante.**

**★★ Et l'art. 53 inscrit la satisfaction, donc la guérison efface l'année du décompte de l'art. 59 :** *« une société ayant manqué deux années dont la plus récente est encore guérissable peut, par ce seul geste, **sortir de l'exposition à la radiation** »*.
⚠️ **Une seule année guérissable à la fois** — le compte à rebours ne concerne jamais plus d'une ligne.
⚠️ **Et le retrait déplace le dénominateur de 6** si le geste passe par le site 4. **Décision produit non tranchée.**

---

## 9u. ✅✅ L'AXE DE CONSÉQUENCE — LIVRÉ `5721871` + `22b9fa9` (2026-08-14). Et le défaut d'affordance qu'il a révélé.

**LA MESURE QUI A RENVERSÉ L'ORDRE.** WICK, `deadline:fed_annual_return:anniversary`, par id, à deux horloges : au **2026-08-11** `d=+57` · `live` · score **0.1500** · **RANG 1**. Au **2026-12-01** `d=−55` · `regularize` · score **1.0000** · **RANG 9**. Le jour où l'obligation devient réellement en retard, son score atteint son **MAXIMUM** et son rang **SORT de la coupe des cinq**, derrière huit lignes non échues dont quatre pour un exercice qui n'a pas commencé. **Le board cachait l'obligation au moment précis où elle comptait.**

### C1 `5721871` — LE MODULE, INERTE ET GATÉ SEUL

`lib/obligations/consequence.ts`, **zéro importateur**. Livré à part parce que l'échelle est une **affirmation de droit** et qu'elle devait être lue seule avant de pouvoir déplacer une ligne de board.

**Quatre niveaux qui nomment un MÉCANISME, pas un grade** — `none` · `penalty` (art. 87 LPLE) · `default` (art. 263 + art. 212(1)a)(iii) LCSA) · `strikeoff` (art. 59 al. 1 LPLE). *`BAS/MOYENNE/ÉLEVÉE` aurait été une opinion ; ceux-là se vérifient contre un texte.* **Chaque constante nomme son article À SA LIGNE**, jamais dans un docblock d'en-tête : le voisin `REMEDIATE_THRESHOLD_YEARS = 2` est une **convention 🟡 révisable**, celles-ci sont du **droit 🟢**, et les deux modules se ressemblent.

**★ L'INCONNU N'EST PAS `none`, ET LE TYPE L'IMPOSE.** Le refus est un **ARM séparé** de `ConsequenceAssessment`, pas un cinquième niveau. **[MESURÉ — le gate de ce ship est `tsc` PROUVÉ REJETANT, parce qu'un artefact immobile ne prouve rien sur un fichier sans importateur.]** Une sonde a produit **TS2322** sur un niveau illégal et **TS2339** sur un accès à `.level` sans narrowing sur `known` : *« Property 'level' does not exist on type '{ readonly known: false; … }' »*. Aucun appelant ne peut lire un niveau qu'il n'a pas d'abord narrowé.

⚠️ **CE QU'IL NE SAIT PAS CALCULER :** tout ce qui passe par la **mise en demeure de l'art. 73 LPLE**. Cette date **n'existe nulle part** — ni sur `companies`, ni au catalogue, ni dans un feeder. La branche `strikeoff` dit que l'exposition est **OUVERTE** ; elle ne dira jamais qu'elle est **PROCHE**. *Interdit explicite d'ajouter un champ : une date inventée serait pire que pas de date.*

### C2 `22b9fa9` — LA VOIE, ET POURQUOI LA DÉRIVATION EST PAR ÉCHELLE

**UNE comparaison, AVANT le seau.** Une ligne dont la conséquence est `strikeoff` ou `default` précède une ligne qui ne l'est pas. **À L'INTÉRIEUR de chaque groupe l'ordre existant est INCHANGÉ, seau compris** — rien n'est repondéré, rien n'est descendu, et c'est pourquoi une conséquence **inconnue** y siège sans dommage : elle ne promeut simplement pas. Seuls ces deux paliers qualifient — **l'EXISTENCE de la société y est en jeu**.

**★ C'est la seconde moitié de la règle de Dom du 2026-07-05**, en entier : *« éviter de prendre du retard SI ET SEULEMENT SI les items à haute conséquence n'y sont plus. »* Le seau a toujours été la première moitié. **La condition ne pouvait pas s'écrire avant que la conséquence existe comme axe.**

**★★ LA DÉRIVATION EST PAR ÉCHELLE, ET C'EST UNE DIFFÉRENCE DE FORME DES DONNÉES, PAS UN CAS PARTICULIER.** Le Québec compte des **ANNÉES** : `today.getFullYear() - Y >= 2`, qui observe au 1ᵉʳ janvier de Y+2 — **après toute échéance art. 45 possible** (fin d'exercice + 6 mois, donc au plus tard juin de Y+1), quelle que soit la fin d'exercice et **sans avoir à la connaître**. ⚠️ **Cela SOUS-DÉCLARE d'au plus un an, délibérément** : un faux `strikeoff` envoie chez un avocat pour rien, un tardif décale une promotion. **Ce n'est PAS `liveness`**, dont la branche B bascule au 1ᵉʳ janvier et diverge de l'art. 45 de **jusqu'à 332 jours** pour un exercice décembre.

Le fédéral lit **SA PROPRE HORLOGE** (`daysUntilDue < 0`) parce qu'il le doit. **[MESURÉ 2026-08-13 : les HUIT lignes `cbca_req_annual_update_qc` de Wick portent `daysUntilDue: null`, historiques comprises — il n'existe aucune horloge côté QC. Et `obligationFiscalYear` ne rend jamais une année passée, donc l'écart `today.getFullYear() - year` de la ligne fédérale vaut toujours 0 ou 1 : un compte d'années ne pourrait JAMAIS y atteindre 2.]**

**UNE SEULE LIGNE PROMUE PAR OBLIGATION** — la plus récente en défaut. La conséquence est une propriété de **l'ÉTAT DE LA SOCIÉTÉ** sur une obligation, pas de chaque ligne-année : Wick a cinq années REQ en défaut et Acme six, et toutes les promouvoir remplirait un board de cinq lignes de rangées sans action utile séparée (**art. 52 — une seule année guérissable à la fois**).

### ★★ FACT 7 EST PASSÉ INERT À SA PREMIÈRE ÉCRITURE — LE DEUXIÈME DE SUITE

**[MESURÉ : avec ses deux premières sondes, élargir la voie pour promouvoir TOUS les niveaux connus laissait le fait VERT, message byte-identique.]** Le défaut n'était pas l'assertion mais son **SUJET** : à la première horloge **aucune des deux sondes n'est EN DÉFAUT**, donc les deux quittent `promotedRowIds` sur `inDefault.length === 0` — **AVANT que le filtre de niveau soit atteint**. L'assertion vérifiait qu'un **ENSEMBLE VIDE** ne promeut pas : vrai par construction, muet sur les niveaux.

**★ LA TROISIÈME SONDE EXISTE POUR ATTEINDRE LE FILTRE ET Y ÊTRE REFUSÉE.** Elle est **en défaut** (une année manquée), son niveau est **`penalty`** — non promouvant — et elle siège dans le **seau INFÉRIEUR**, parce que promouvoir à tort une ligne déjà première ne la déplacerait pas et rendrait l'assertion vide une seconde fois, autrement. *Une sonde sans `requirementKey` n'aurait jamais atteint `byReqKey` et aurait été refusée sur sa FORME ; celle-ci l'est sur son NIVEAU.*

**Sept préconditions**, chacune échouant par son nom et disant ce qu'elle empêche. **Preuve à trois états, deux sens de rejet :** voie retirée → **✗ assertion (1)** à la seconde horloge, `#2 derrière #1` · voie élargie → **✗ assertion (2)** à la PREMIÈRE horloge, `#1 devant #2` · arbre réel → **✓**. Les six autres FACTS n'ont bougé sous aucune mutation.

**★★ ET C'EST LE DEUXIÈME FAIT DE SUITE À PASSER INERT, POUR UNE CAUSE DIFFÉRENTE. → §109**
> **FACT 6** — le sujet produisait la **bonne réponse par un AUTRE mécanisme** (`id.localeCompare`, `'d' < 's'`).
> **FACT 7** — le sujet **n'atteignait JAMAIS le mécanisme testé**.
> *Deux formes d'inertie, une seule chose qui les distingue : la preuve de rejet. Un fait vert qui n'a jamais dit non n'est pas un garde, c'est un ornement.*

### ★★ LE HÉROS, ET LE DÉFAUT D'AFFORDANCE ANTÉRIEUR QUE C2 A RÉVÉLÉ

Le garde `liveness === 'live'` est retiré, et le `console.warn` derrière lui **supprimé plutôt que réécrit** — sa condition ne peut plus se produire. **[MESURÉ 2026-08-14, quatre fixtures × deux horloges : 5 combinaisons sur 8 mettent une ligne non-`live` au rang 1.]** Avec le garde en place, le board ne rendait **AUCUN héros** sur **trois fixtures sur quatre** — cinq cartes identiques, et un board bâti pour dire *« commence ici »* ne disait rien. **Le héros imprime désormais `tierBadge`**, qu'il omettait : *la ligne la plus grave doit être celle dont la gravité se VOIT, pas la seule à la cacher.*

**★★ ET UN DÉFAUT PLUS ANCIEN, QUE CE LOT A RENDU VISIBLE ET CORRIGE. → §111**

Le board décidait **quoi OFFRIR à partir de la GRAVITÉ**. Une branche `if (isRemediate)` précédait toute la chaîne de verbes et remplaçait l'action réelle par un `<button>` **inerte** — aucun `onClick`, aucun `href`, `cursor-default` dans sa propre classe, mesuré : il ne faisait rien tout en étant focusable et annoncé comme bouton. Et `showHowTo = isFilingRow && !isRemediate` retirait **AUSSI** la pastille « Comment faire ? » : **DEUX affordances jetées, pas une.**

**La donnée était CORRECTE de bout en bout** — `completeness:cbca_req_annual_update_qc:2024` de Wick arrive avec `actionKind: 'upload'`, `canUpload: true`, `canGenerate: false`. **Complétude offre Téléverser sur CETTE MÊME LIGNE**, et `grep remediate` dans `RequirementRow.tsx` rend **ZÉRO** : elle lit ce qui est POSSIBLE et ne consulte jamais le seau. **Deux surfaces, une ligne, réponses opposées — la forme de `6f0a48c` sur une quatrième ligne de code.**

⚠️ **LE DÉFAUT PRÉCÉDAIT LA VOIE.** `probe-dec.txt` et `probe-c2b.txt` portent des champs **IDENTIQUES** sur cette ligne. Il vivait **au rang 28**, où personne ne le voyait. **Le commit qui révèle un défaut est celui qui le corrige.**

> **★★ LA RÈGLE QUI TRANSFÈRE : DÉCIDER QUOI OFFRIR À PARTIR DE CE QUI EST POSSIBLE, JAMAIS DE LA GRAVITÉ.** La sévérité est déjà dite **une fois**, par `tierBadge`. *Un verbe qui disparaît quand la ligne empire retire le seul moyen de la réparer.* « Consulter un professionnel » est désormais une **mention `<span>` À CÔTÉ** du verbe, pas un contrôle qui ment.

### LES CHIFFRES, PAR FIXTURE — comparaison PAR ID, deux horloges

| fixture | promues 2026-08-11 | promues 2026-12-01 | ligne promue |
|---|---|---|---|
| **WICK** | 1 | **2** | `…req_annual_update_qc:2024` (28→1) · `fed_annual_return` (9→1) |
| **ACME** | 1 | 1 | `completeness:lsaq_req_annual_update:2024` (17→1) |
| **CAFEDUCOIN** | 0 | **1** | `deadline:fed_annual_return:anniversary` (8→1) |
| **FIXTURECAP** | 0 | 0 | — |

**★ TOUS LES NOMBRES SONT IDENTIQUES, SEUL L'ORDRE BOUGE.** `ranked.length` identique **24 fois sur 24** · ensembles d'ids identiques · les quatre compteurs du verdict, `requirementsTotal` et le pourcentage : **`diff` sur les agrégats → aucune différence**. Le déplacement est une **ROTATION** : une ligne saute, les autres glissent d'exactement +1 (+2 là où deux sont promues), et les lignes déjà sous la promue **ne bougent pas du tout**.

⚠️ **ACME est le contrôle négatif FÉDÉRAL (LSA, aucune ligne `cbca_annual_return`) mais PAS un contrôle neutre** : elle porte **six années QC consécutives manquées** et est promue sur l'échelle québécoise. *Une fixture peut être négative sur un axe et positive sur l'autre — le dire évite de lire son mouvement comme une fuite.*

**LE HARNAIS A ÉTÉ RÉPARÉ DANS LA MÊME SESSION.** Il appelait `rankObligations` avec **deux arguments** et était **mort depuis le câblage**, en silence, parce que `tsc` ne voit pas un fichier hors dépôt. Il a aussi gagné l'horloge **`2026-12-01`** — sans elle il ne pouvait pas voir ce lot. *La sauvegarde a été rafraîchie APRÈS la caméra, jamais avant : tant que rien n'est publié et vu, l'ancien état est le seul entièrement vérifié.*

### ⚠️ CE QUI RESTE OUVERT

- **C3 — l'assemblée annuelle descend.** Non scopé.
- **C2d — le marqueur `Formalité à produire · Exercice {année}`** sur les lignes de formalité **sans échéance**. **[MESURÉ : 15 lignes sur les quatre fixtures.]** `ObligationMarker` n'a **pas** besoin de changer (`deadline: string`, aucune sémantique de date dans son rendu) — mais desserrer `isFilingRow` toucherait aussi `showHowTo` et la suppression de `dueLine`, et il faut **deux clés i18n neuves, FR et EN**.
- **Le traitement visuel d'un héros en retard** — décision produit ouverte, **Dom + Aria**. Le rang 1 est désormais toujours le héros, quel que soit son seau.
- **★ Le lot (iii) — et il a DEUX chemins, pas un.** Faire émettre N lignes fédérales ne suffit pas : **le catalogue détient l'appariement des documents**. Un reçu ne se rattache qu'à `(requirement_key, requirement_year)`, et l'année d'attache de la ligne anniversaire est **UNE** valeur. Sans une seconde décision côté catalogue, N lignes seraient visibles et **infermables individuellement**.

---

## 9. IF THIS THREAD DIES — start here

1. Boot protocol, then the seven ZK files, then the read-back. Do not skip it.
2. Read this document, then `docs/a3-ranking-brain-spec-2026-07-03.md` and `a3-obligation-contract-design-2026-07-02.md`.
3. **Do not re-run the investigations in §3.** They are measured, at `e9bc5fb`, and they cost three hours. Re-anchor line numbers; do not re-derive facts.
4. **Ask Dom for the two §4 decisions before scoping anything.**
5. The instruments exist — `Max - CTO/baselines/probe-consolidation.ts`, parameterised by clock, plus capture sets. **Do not build a new one.**
6. **Nothing in this arc is launch-gating.** A1 (lawyer content GREEN) and the signup toggle are. The pace is Dom's.
